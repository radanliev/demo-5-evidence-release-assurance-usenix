# science.stale_artifact: result artifacts

## Finding

`results/benchmark_summary.json` and `results/comparative_evaluation.json` changed after the manuscript was last edited. Their dependent claims, table entries, and generated figures must be reconciled before publication.

## Correct value or claim

No claim revision is justified without auditing every manuscript dependency against a versioned, reproducible result artifact. The generated benchmark file also lacks repeated-run support for its performance statistics. The current artifacts record `39.081 ms` and `0.1206%` at 100,000 traces, `5,841.27` operations/second at eight workers, and comparative local-predicate rates of 0.0%, 25.0%, 25.0%, and 100.0%.

## Exact proposed diff

```diff
- While processing policy gate evaluations at up to 5,883.86 operations/second across 8 parallel cores, scaling Merkle tree construction for $N=100,000$ execution traces in $39.59\text{ ms}$ (representing a packaging overhead of 0.1221\%) with $O(\log N)$ sparse proof compression.
+ While processing policy gate evaluations at up to 5,841.27 operations/second across 8 parallel cores, scaling Merkle tree construction for $N=100,000$ execution traces in $39.081\text{ ms}$ (representing a packaging overhead of 0.1206\%) with $O(\log N)$ sparse proof compression.
```

## Evidence needed

A claim-to-artifact audit mapping every manuscript result, table cell, figure, and caption to a versioned producer output. Preserve repeated-run performance samples and environment metadata before approving performance wording or precision. Confirm whether the comparative entries describe named systems or only local predicates. A human must approve every scientific revision.
