# science.number_mismatch: packaging overhead

## Finding

`docs/usenix_paper_manuscript.tex:59` reports a packaging overhead of `0.1221\%`, while the newly generated `results/benchmark_summary.json::merkle_scaling[4].packaging_overhead_pct` records `0.1243`.

## Correct value or claim

No revised manuscript value is justified. The benchmark uses a synthetic, code-assigned execution-duration denominator and has no repeated-run artifact, so neither the prior nor newly generated value supports an operational overhead claim.

## Exact proposed diff

```diff
- ... (representing a packaging overhead of 0.1221\%) ...
+ ... (representing a packaging overhead of 0.1243\%) ...
```

## Evidence needed

First determine whether the artifact is authoritative by rerunning its producing benchmark with a documented, versioned environment. Preserve repeated raw samples, warm-up and exclusion protocol, and measured execution-duration data for the stated workload. The current artifact is only a single synthetic-denominator observation, so a human must approve both this numerical update and whether the operational-overhead wording remains justified.
