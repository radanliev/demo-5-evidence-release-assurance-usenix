# science.claim_precision: performance values

## Finding

Fresh benchmark executions differ from the exact stored latency and throughput values, and the artifacts contain no repeated-run uncertainty evidence.

## Correct claim

No revised exact value is justified from the current evidence.

## Exact proposed diff

```diff
- ... up to 5,883.86 operations/second ... 39.59\text{ ms} (representing a packaging overhead of 0.1221\%) ...
+ [Replace with human-approved rounded central estimates, intervals, and a measured speedup derived from preserved repetitions.]
```

## Evidence needed

Raw repeated timing samples and environment metadata for every trace-count and worker configuration.
