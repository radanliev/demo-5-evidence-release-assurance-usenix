# science.number_mismatch: packaging overhead

## Finding

`docs/usenix_paper_manuscript.tex:59` reports a `0.1221%` packaging overhead, while `results/benchmark_summary.json::merkle_scaling[3].packaging_overhead_pct` currently records `0.1208`.

## Correct value or claim

The named artifact entry currently records `0.1208%` at 10,000 traces. The manuscript pairs its overhead statement with the 100,000-trace result, whose current artifact entry is `0.1206%`; the intended trace-count mapping and synthetic denominator must be resolved before approval.

## Exact proposed diff

```diff
- (representing a packaging overhead of 0.1221\%)
+ (representing a packaging overhead of 0.1206\%)
```

## Evidence needed

Confirm which trace-count result the abstract is intended to report, preserve repeated timing samples, and establish a measured end-to-end trace-execution denominator rather than the current code-assigned synthetic duration. A human must approve any resulting claim revision.
