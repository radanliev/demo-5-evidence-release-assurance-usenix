\documentclass[letterpaper,10pt,twocolumn]{article}
\usepackage{usenix}
\usepackage{lmodern}

\usepackage{amsmath,amssymb,amsfonts,amsthm}
\DeclareMathAlphabet{\mathcal}{OMS}{cmsy}{m}{n}
%\renewcommand{\ttdefault}{cmtt}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{url}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{subcaption}
\usepackage{listings}
\usepackage{enumitem}
\usepackage{tikz}
\raggedbottom
\usetikzlibrary{shapes.geometric, arrows, positioning, fit, backgrounds, calc}
% NOTE: no \setlist whitespace compression. The USENIX Security '27 CFP:
% "Any attempts to remove whitespace (e.g., negative vspaces, savetrees,
%  titlesec, removing author blocks, etc.) or change the formatting defaults
%  (e.g., fonts) are forbidden and may result in rejection."
% Page limits are met by cutting content, which is what the CFP requires.

\lstdefinelanguage{json}{
  stringstyle=\color{red},
  morestring=[b]",
  morestring=[d]'
}

\lstset{
  basicstyle=\ttfamily\scriptsize,
  breaklines=true,
  frame=single,
  captionpos=b,
  keywordstyle=\color{blue},
  stringstyle=\color{red},
  commentstyle=\color{gray}
}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=blue!70!black,
    urlcolor=blue!80!black
}

\theoremstyle{definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{definition}{Definition}

% Extra inter-word stretch before TeX resorts to an overfull line.
% Changes no page geometry.
\setlength{\emergencystretch}{3em}
\sloppy

% 2026-08-16: corpus_evaluation.json regenerated (note wording); Layer 1/2/3
% numbers re-confirmed against the artifact (test_corpus_eval_json.py binds them).
\input{frozen_metrics}
\input{security_metrics}
\input{artifact_digest}

\begin{document}

\title{\Large \bf Evidence-Backed Release Assurance for Autonomous Agent Deployments:\\
Cryptographic Attestation, Fail-Closed Gates, and Tamper-Resilient Audit Infrastructure}

% ---------------------------------------------------------------------------
% AUTHOR BLOCK -- ANONYMITY SWITCH
%
% USENIX Security '27 is DOUBLE-BLIND. The CFP is explicit:
%   "Papers must be submitted for anonymous review: no author names or
%    affiliations may appear on the title page and authors must not reveal
%    their identity in the text."
%   "Papers that are not properly anonymized may be rejected without review."
%
% So the submission build MUST stay anonymous. The real author block lives in
% the \else branch and is used for the camera-ready, arXiv, and internal
% copies. Flip ONE line to switch; never edit the block itself for a build.
%
%   \anonymoustrue   -> submission        (DEFAULT -- upload this to HotCRP)
%   \anonymousfalse  -> camera-ready      (after acceptance)
%
% Full contact details, roles and affiliations are recorded in AUTHORS.md,
% which is deliberately excluded from the anonymous review artifact.
% ---------------------------------------------------------------------------
\newif\ifanonymous
\anonymoustrue          % <-- SUBMISSION BUILD. Set to \anonymousfalse for camera-ready.

\ifanonymous
  \author{{\rm Anonymous Submission}}
\else
  \author{
  {\rm Petar Radanliev}\\
  The Open University\\
  The Alan Turing Institute\\
  \normalsize\nolinkurl{radanliev@gmail.com}
  \and
  {\rm Omar Santos}\\
  Cisco Systems\\
  Security \& Trust, Cisco PSIRT\\
  \normalsize\nolinkurl{osantos@cisco.com}
  \and
  {\rm Carsten Maple}\\
  University of Warwick\\
  The Alan Turing Institute\\
  \normalsize\nolinkurl{carsten.maple@warwick.ac.uk}
  }
\fi

\maketitle

\begin{abstract}
Autonomous agents are promoted to production on the strength of what their execution traces say they did. Every deployed attestation scheme for such traces --- in-toto's runtime-trace predicate, SLSA, TUF, the 2026 agent-provenance drafts, and the per-action receipt schemes that have converged this year --- shares a blind spot: the evidence collector is the sole observer, so an adversary who controls the recording path need not forge anything. It simply does not record. The resulting bundle is internally consistent, every signature verifies, and every integrity check passes, because nothing was tampered with.

This paper makes that gap precise and closes it. We define \emph{Witnessed Trace Completeness}, a property relative to a declared set of witnesses --- the tool endpoints, API gateways and sandbox brokers that actually serve agent actions. Each witness issues receipts carrying a monotonic per-session sequence number and, at session end, a signed commitment to how many actions it served. A release gate then \emph{reconciles} rather than merely verifies. We prove that under EUF-CMA an adversary holding the agent and the collector, but no witness key, cannot omit a witnessed action; and we prove a \emph{separation} result: no scheme whose verdict is a function of a single submitted document can achieve either witnessed completeness or replay-freedom, which places every record-format standard on the wrong side of a formal boundary rather than merely behind on features.

Against \omVectors{} omission attacks, in-toto/DSSE and TUF detect \omDsse{}, per-issuer hash chaining \omChain{}, per-action receipts \omReceipts{}, and \textsc{EviAssure} without reconciliation \omNoWtc{}; with reconciliation it detects \omWtc/\omVectors{}, while an honestly witnessed execution is still approved. On the orthogonal tamper axis it blocks \eviBlocked/\eviTotal{} vectors (\eviPct\%, 95\% CI [\eviCIlo, \eviCIhi]) against \compBlocked{} for an executed in-toto/DSSE + TUF + OPA composition, and we report the one vector it fails. A held-out evaluation over \corpusProfiles{} profiles recalls \insOvertK/\insOvertN{} structurally overt behavioural anomalies and \insStealthK/\insStealthN{} semantically disguised ones, marking where cryptographic release assurance stops and content-level policy must begin.
\end{abstract}

\section{Introduction}
Autonomous agents driven by Large Language Models (LLMs) act across tool environments, API endpoints and persistent memory stores~\cite{jimenez2024swebench, greshake2023not, yao2023react}. Unlike a compiled artifact, whose behaviour is fixed by its code, an agent exhibits non-deterministic reasoning, dynamic tool invocation, and adaptive multi-step decision sequences. When such an agent is promoted to production --- an updated checkpoint, a widened tool scope, a new pipeline stage --- organisational security rests on verifying that safety invariants, authorization constraints and regression suites actually held \emph{during that execution}~\cite{greshake2023not}.

Release pipelines verify no such thing. Deployment gates evaluate unauthenticated status signals --- process exit codes, plain-text console logs, unsigned JSON build-status files --- emitted by intermediate runner environments. An attacker who controls a build step, manipulates agent memory, or exploits a prompt-injection path can forge an approval signal outright, bypassing verification entirely~\cite{torres2019in-toto, slsa2023supply, samuel2010surviving, cappos2008look}. That such paths are exploited routinely is well established: taxonomies of documented supply-chain attacks enumerate hundreds of real incidents~\cite{ladisa2023sok, okafor2022sok}, large-scale measurement finds package-manager compromise commonplace in interpreted-language ecosystems~\cite{duan2021towards}, and multi-repository update systems require dedicated defences~\cite{moore2023artemis}.

The gap is one of subject rather than of strength. Supply-chain security attests the origin and integrity of \emph{static} artifacts --- container images, compiled binaries, model weight files --- and does so well. But an agent's security posture is not defined by its code artifact alone; it is defined by the dynamic, non-deterministic execution trace that artifact produced against a test suite. We can cryptographically attest \emph{what} an agent is. We cannot, today, authorize a release on \emph{how it behaved}.

To address these critical vulnerabilities, EviAssure\footnote{The complete anonymous source code, evaluation corpus, and benchmark suite are available at \url{https://anonymous.4open.science/r/eviassure-release-assurance/}.} introduces an evidence-backed release assurance architecture that fills this niche. It replaces informal release signals with cryptographically authenticated Evidence Bundles backed by Merkle inclusion proofs, timestamp freshness windows, digital signatures, and provenance envelopes.

\subsection{Key Contributions}
The primary contributions of this work are:
\begin{enumerate}
    \item \textbf{Witnessed Trace Completeness}: a security property, and a protocol achieving it, for the attack class every existing agent-attestation scheme leaves open --- the collector that omits rather than forges. Sequence-bound witness receipts plus a signed closing count make omission require a witness-key forgery. We state the guarantee \emph{relative to a declared witness set} and prove it (Theorem~\ref{thm:wtc}); actions outside that set remain undetectable, and we say so.
    \item \textbf{A separation between record formats and authorization layers}: Theorem~\ref{thm:separation} shows that any scheme whose verdict is a function of one submitted document cannot achieve witnessed completeness or replay-freedom, for information-theoretic rather than engineering reasons. This converts a positioning argument that earlier drafts of this paper made rhetorically into a proof.
    \item \textbf{Evidence-bound release authorization}: a fail-closed gate whose verdict is a deterministic, reproducible function of attested evidence under a pinned policy, with domain-separated (RFC 6962) depth-bound Merkle trace attestation and truncation-resistant count binding.
    \item \textbf{A falsifiable evaluation, including where the system fails}: an omission suite with an honest-execution control, \scoredVectors{} tamper vectors of which five were authored against the design rather than the check list (three broke earlier revisions), executed in-toto/DSSE, TUF, hash-chaining and per-action-receipt baselines, clean negative controls, and a held-out inspection protocol. Every rate carries a Wilson interval, and we report one tamper vector and one detector blind spot that the system does not close.
\end{enumerate}

\paragraph{Record Formats versus Authorization Layers.}
A distinction runs through this paper. in-toto provides signed, tamper-evident attestations of supply-chain steps, including vetted runtime-trace predicates~\cite{intoto_runtime_trace}, and SLSA builds graduated release provenance on top~\cite{torres2019in-toto, slsa2023supply}. These frameworks define \emph{passive record formats}: they attest what occurred, but supply no \emph{stateful authorization layer} that gates a release on a non-deterministic execution trace. EviAssure is that layer --- an active, fail-closed gate --- and Theorem~\ref{thm:separation} establishes that the distinction is formal rather than rhetorical. Concretely, EviAssure adds three enforcement mechanisms these specifications omit: (i) truncation-resistant trace binding via a signed trace count and, under Definition~\ref{def:wtc}, witness reconciliation; (ii) freshness and replay protection held as gate \emph{state} rather than asserted in a document; and (iii) a reproducible release decision under a pinned policy version. Composition is preserved: an \texttt{EvidenceBundle} is an in-toto v0.2 / SLSA v1.0 envelope, so existing records ride inside it unmodified. Section~\ref{sec:related-2026} compares against the 2026 agent-provenance standards in detail.

\paragraph{Open Science and Reproducibility.}
The full implementation, the \corpusProfiles{}-profile execution trace corpus, the \scoredVectors{}-vector attack harness with its clean negative controls, and the executed baselines (in-toto/DSSE, TUF, and the Open Policy Agent (OPA) Rego policy) are released as a sanitized anonymous review artifact (Appendix~\ref{sec:appendix_open_science}).

\section{Preliminaries}\label{sec:preliminaries}
\subsection{Formal Security Definitions}

\begin{definition}[Unforgeability under Chosen Message Attack (EUF-CMA)]
An Evidence Assurance Scheme $\Sigma = (\text{KeyGen}, \text{Package}, \text{Verify})$ is EUF-CMA secure if for all probabilistic polynomial-time adversaries $\mathcal{A}$, the probability that $\mathcal{A}$ outputs a valid pair $(E^*, \sigma^*)$ such that $\text{Verify}(PK, E^*, \sigma^*) = 1$ without $E^*$ having been signed by an honest party holding $SK$ is negligible in security parameter $\lambda$:
\begin{equation}
\text{Pr}[\text{Exp}_{\Sigma, \mathcal{A}}^{\text{euf-cma}}(\lambda) = 1] \le \text{negl}(\lambda)
\end{equation}
\end{definition}

\begin{definition}[Merkle Trace Inclusion Soundness]
For root $R_M$ committed together with its leaf count $N$, and leaf $H_i$, a Merkle proof $\pi_i$ is sound if no computationally bounded adversary can construct $\pi'_i$ for any $H'_i \neq H_i$ --- including internal node digests of the same tree --- such that $\text{VerifyMerkle}(H'_i, \pi'_i, R_M, d) = 1$ for the committed depth $d = \lceil\log_2 N\rceil$, where verification accepts only proof paths of exactly length $d$ and leaves and internal nodes are hashed under disjoint domain prefixes.
\end{definition}

\begin{definition}[Evidence Integrity]\label{def:integrity}
An evidence pack $(R_M, \sigma, N)$ comprising a Merkle root $R_M$, signature $\sigma$, and signed trace count $N$ provides \emph{evidence integrity} if no probabilistic polynomial-time adversary, given oracle access to the honest packager, can produce a verifying pack $(R'_M, \sigma', N')$ that the packager never issued; equivalently, nothing that was \emph{recorded} can be modified, reordered, inserted, or removed without detection. Integrity is a property of the signed record and is guaranteed by Theorem~\ref{thm:failclosed} below.
\end{definition}

\begin{definition}[Trace Completeness]\label{def:completeness}
Let $E$ be an agent execution and let $\mathcal{C}$ be the evidence collector that observes $E$. A recorded trace set $T = \{T_1, \dots, T_N\}$ is \emph{complete with respect to $E$} if every action of $E$ that $\mathcal{C}$ observed is represented by exactly one $T_i$. Completeness is an observation property of the \emph{collector}, not of the Merkle tree: the tree attests exactly the leaves it was given. If a real execution is $A \rightarrow B \rightarrow C \rightarrow D \rightarrow E$ but the recorded trace is $A \rightarrow B \rightarrow D \rightarrow E$, the tree protects the recorded sequence perfectly and says nothing about $C$. No signature, hash chain, or inclusion proof over the submitted set can, because the adversary forged nothing --- it declined to create evidence. Definition~\ref{def:wtc} makes this attainable by moving the observation out of the collector's trust domain.
\end{definition}

\begin{definition}[Witnessed Trace Completeness]\label{def:wtc}
Let $\mathcal{W}=\{W_1,\dots,W_m\}$ be a set of \emph{witnesses}, each holding a signing key the adversary does not control, and let $\mathsf{med}(W_j)$ be the set of action types $W_j$ mediates. An action is \emph{witnessed} if its type lies in $\bigcup_j \mathsf{med}(W_j)$. A submitted trace set $T$ for session $\mathit{sid}$ is \emph{witness-complete} if for every $W_j \in \mathcal{W}$ there is a $W_j$-signed closing $(\mathit{sid}, n_j)$, the $W_j$-signed receipts in $T$ are exactly the contiguous sequence $1,\dots,n_j$ for $\mathit{sid}$, each receipt binds to a distinct member of $T$, and every witnessed action in $T$ carries a receipt.

Completeness is stated \emph{relative to} $\mathcal{W}$: an action of a type no witness mediates is outside the guarantee. This is a deployment-controllable condition --- mediate every externally observable effect --- rather than the unbounded assumption that the collector observed everything.
\end{definition}

\begin{theorem}[Witnessed Completeness Soundness]\label{thm:wtc}
Let $\mathcal{A}$ control the agent, the evidence collector and the collector's signing key, but no key in $\mathcal{W}$. If the gate accepts a witness-complete bundle for session $\mathit{sid}$, then except with probability $m \cdot \mathrm{Adv}^{euf\text{-}cma}_{\mathrm{Ed25519}}$, the trace set contains every witnessed action performed in $\mathit{sid}$.
\end{theorem}

\begin{proof}
Suppose $\mathcal{A}$ omits a witnessed action $a$ served by $W_j$ as its $k$-th action, and the gate accepts. $W_j$'s counter is monotone and advances only on service, so $W_j$ issued receipts $1,\dots,n_j$ with $k \le n_j$, and its closing commits to $n_j$. Acceptance requires the receipt sequence for $W_j$ to be exactly $1,\dots,n'_j$ with a matching closing. Three cases. (i) $n'_j = n_j$: then $\mathcal{A}$ supplied $n_j$ distinct receipts, but only $n_j - 1$ correspond to submitted actions, so some receipt is unmatched and reconciliation rejects --- unless $\mathcal{A}$ minted a receipt, an Ed25519 forgery under $W_j$. (ii) $n'_j < n_j$: the presented closing differs from the one $W_j$ signed, so $\mathcal{A}$ forged a closing under $W_j$. (iii) $\mathcal{A}$ substitutes a receipt from another session $\mathit{sid}' \neq \mathit{sid}$: the session identifier is inside the signed receipt payload, so the substitution is rejected without forgery. Each surviving case is a forgery against one of $m$ witness keys; a union bound gives the stated advantage. Fabrication of a witnessed action is symmetric: the action carries no receipt, and minting one is again a forgery. $\square$
\end{proof}

\begin{theorem}[Record Formats Cannot Achieve These Properties]\label{thm:separation}
Let $V$ be a \emph{stateless document verifier}: a deterministic predicate $V(d) \in \{\mathrm{accept},\mathrm{reject}\}$ over a single submitted document $d$, with access to fixed public parameters (trust roots, policy) but no state that varies with prior submissions and no channel to $\mathcal{W}$. Then $V$ achieves neither replay-freedom nor witnessed completeness.
\end{theorem}

\begin{proof}
\emph{Replay-freedom.} Determinism gives $V(d)=V(d)$: if $V$ accepts $d$ once it accepts every resubmission, so an adversary replays an approved release indefinitely. Distinguishing first from subsequent submission requires state that varies across submissions, which $V$ by definition lacks.

\emph{Witnessed completeness.} Fix a witness $W$ and consider two executions: $E_1$ serving $n$ actions, and $E_2$ serving the same first $n-1$ and then a further action whose record the adversary omits. Let $d_1, d_2$ be the honestly-signed documents. Every attestation in $d_2$ is genuine: the collector signs truthfully over what it recorded, and each retained receipt is authentic. So $d_1$ and $d_2$ are identically distributed over every field $V$ can inspect, and $V(d_1)=V(d_2)$. Since $V$ must accept $d_1$ (a correct execution), it accepts $d_2$. The only field that could differ is a commitment to $W$'s served count, which $W$ alone can produce --- and requiring it makes the verifier's input depend on a trust domain outside $d$, contradicting statelessness.
\end{proof}

Theorem~\ref{thm:separation} is why we describe in-toto, SLSA, TUF, AAS-1 and \texttt{agent-decision} as record formats rather than as weaker gates. Their verification is by construction a function of the submitted document; the gap is structural, not a matter of maturity, and Section~\ref{sec:omission} measures it.

\section{System Architecture and Threat Model}\label{sec:system_threat}

\textbf{Intuition.} EviAssure turns an agent's step-by-step execution path into a signed, Merkle-committed evidence bundle before deployment, and evaluates that bundle at a \emph{fail-closed gate} that denies release whenever a required proof, signature, or threshold is missing, malformed, or compromised.

\subsection{System Model}
The EviAssure release assurance architecture involves four primary entities: the \emph{Evidence Collector}, the \emph{Key Management System (KMS)}, the \emph{Policy Registry}, and the \emph{Release Assurance Gate} (Figure~\ref{fig:architecture}).

\textbf{Evidence Collector}: in production, an out-of-process monitor outside the agent's sandbox, observing activity via eBPF syscall tracepoints and container network proxies. \emph{In this evaluation it is Python instrumentation wrappers} --- the paper's principal validity threat (Section~\ref{sec:limitations}).
\textbf{Cloud KMS / HSM}: signing authority is rooted outside the runner; the collector obtains signatures over canonical digests without holding key material. The gate checks the declared ARN against \texttt{kms\_key\_arn\_pattern}, a misconfiguration control rather than an adversarial one (Section~\ref{sec:limitations}).
\textbf{Policy Registry}: an out-of-band store for release policies, pinned public keys, and the revocation list.
\textbf{Release Assurance Gate}: the enforcement entity, running in a trusted CI/CD step, evaluating the signed \texttt{EvidenceBundle} against the pinned policy.

\begin{figure*}[t]
\centering
\begin{tikzpicture}[
    box/.style={rectangle, draw=#1!80!black, fill=#1!12, rounded corners=4pt, minimum width=2.1cm, minimum height=0.85cm, align=center, font=\small, thick},
    gate_box/.style={rectangle, draw=blue!85!black, fill=blue!15, rounded corners=4pt, minimum width=2.2cm, minimum height=0.95cm, align=center, font=\small\bfseries, very thick},
    zone/.style={draw=#1, dashed, rounded corners=6pt, fill=#1!3},
    flow/.style={thick, ->, >=stealth, draw=black!75},
    secflow/.style={very thick, ->, >=stealth, draw=blue!80!black},
    lbl/.style={font=\footnotesize, fill=white, inner sep=1.5pt, rounded corners=1pt, draw=gray!30}
]

% Background Trust Boundary Enclosures (Pure Integer Coordinates)
\begin{scope}[on background layer]
% Zone 1: Untrusted Agent Tier
\draw[zone=red] (0, -2) rectangle (3, 2);
\node[anchor=north, font=\footnotesize\bfseries\color{red!80!black}] at (1, 2) {Untrusted Tier};

% Zone 2: Isolated Attestation Plane
\draw[zone=cyan!70!blue] (4, -2) rectangle (8, 2);
\node[anchor=north, font=\footnotesize\bfseries\color{cyan!80!black}] at (6, 2) {Attestation Plane};

% Zone 3: Zero-Trust Release Gate
\draw[zone=green!60!black] (9, -2) rectangle (13, 2);
\node[anchor=north, font=\footnotesize\bfseries\color{green!60!black}] at (11, 2) {Zero-Trust Gate};

% Zone 4: Production Plane
\draw[zone=green!70!black] (14, -2) rectangle (17, 2);
\node[anchor=north, font=\footnotesize\bfseries\color{green!70!black}] at (15, 2) {Target};
\end{scope}

% Pipeline Entity Nodes (Pure Integer Coordinates)
\node[box=orange] (agent) at (1, 0) {\textbf{Autonomous Agent}\\[1pt]\footnotesize Untrusted Runtime};

\node[box=cyan] (collector) at (6, 1) {\textbf{Evidence Collector}\\[1pt]\footnotesize Isolated Monitor};
\node[box=purple] (kms) at (6, -1) {\textbf{Cloud KMS / HSM}\\[1pt]\footnotesize Hardware Trust Root};

\node[box=gray] (registry) at (11, 1) {\textbf{Policy Registry}\\[1pt]\footnotesize YAML Rules \& CRL};
\node[gate_box] (gate) at (11, -1) {Release Gate\\[1pt]\footnotesize\mdseries Fail-Closed Engine};

\node[box=green!70!black] (prod) at (16, -1) {\textbf{Production}\\[1pt]\footnotesize Cluster};

% Clear, Non-Overlapping Communication Flows
% 1. Agent -> Collector
\draw[flow] (agent.east) -- node[above=2pt, sloped, lbl] {Traces} (collector.west);

% 2. Collector <-> KMS
\draw[secflow] ([xshift=-5mm]collector.south) -- node[left=2pt, lbl] {Digest $H_{\text{trace}}$} ([xshift=-5mm]kms.north);
\draw[secflow] ([xshift=5mm]kms.north) -- node[right=2pt, lbl] {Signature $\sigma$} ([xshift=5mm]collector.south);

% 3. Collector -> Gate (diagonal connection between distinct levels)
\draw[secflow] (collector.east) -- node[above=2pt, sloped, lbl, pos=0.45] {Evidence $\mathcal{E}$} (gate.west);

% 4. Registry -> Gate (straight vertical connection)
\draw[flow] (registry.south) -- node[right=2pt, lbl] {Policy $\mathcal{P}$} (gate.north);

% 5. Gate -> Production Target (straight horizontal connection with 3.0cm clearance)
\draw[flow, draw=green!60!black, very thick] (gate.east) -- node[above=2pt, lbl] {\textbf{APPROVED}} (prod.west);

\end{tikzpicture}
\caption{EviAssure System Architecture and Trust Boundaries. The Evidence Collector captures execution traces across an out-of-process monitor boundary and requests hardware-rooted attestation from Cloud KMS. The Release Assurance Gate evaluates the signed bundle ($\mathcal{E}$) against pinned governance rules ($\mathcal{P}$) from the Registry to authorize production release.}
\label{fig:architecture}
\end{figure*}

\subsection{Adversary Model}\label{sec:threat}
The security model considers a powerful adversary $\mathcal{A}$ attempting to deploy an unapproved, regression-prone, or malicious agent update to production environments~\cite{shostack2014threat, carlini2024poisoning, schuster2021get}. The adversary $\mathcal{A}$ may control intermediate runner environments, network transit layers, or build scripts. 

\paragraph{Trust Model Boundaries.} EviAssure assumes Ed25519 and SHA-256 remain computationally intractable for PPT adversaries, and that the Cloud KMS / HSM faithfully enforces identity access control over private signing keys. It further requires strict environment isolation: an agent that escapes its sandboxed tier to obtain local file access or code execution on the assurance engine or the collector host can forge an evidence payload \emph{prior} to KMS signing. Compromise of the assurance engine's trusted computing base is therefore an explicit out-of-scope limitation --- the cryptographic guarantees hold assuming a tamper-proof control plane. HSM side channels, subversion of provider root keys, and hypervisor rootkits are likewise out of scope. \emph{Witness} keys are a distinct trust domain: Section~\ref{sec:witness} assumes the adversary holds the agent and collector keys but no witness key, and Section~\ref{sec:limitations} states what degrades when that assumption is weakened.

We categorize $\mathcal{A}$'s in-scope capabilities into five classes, mapped to vectors in Table~\ref{tab:tamper}: runner and memory tampering ($\mathcal{A}_1$), network and signature forgery ($\mathcal{A}_2$), replay and time skew ($\mathcal{A}_3$), governance bypass ($\mathcal{A}_4$), and serialization malleability ($\mathcal{A}_5$).
\subsection{Security Requirements}
The construction is proved against four requirements, subject to the trust boundaries above. The evaluation in Section~\ref{sec:eval} is organised to test each in turn.

\begin{enumerate}
    \item \textbf{Evidence Integrity}: no recorded action can be modified, reordered, inserted, or removed without detection (Definition~\ref{def:integrity}).
    \item \textbf{Trace Completeness}: the attestation must bind the count of recorded actions so that truncation is not silent (Definition~\ref{def:completeness}) --- and, because a count the collector itself signs cannot constrain the collector, must be strengthened to hold \emph{relative to a witness set} (Definition~\ref{def:wtc}).
    \item \textbf{Release Authorization Soundness}: the decision must be a deterministic function of signed evidence and pinned policy. No PPT adversary should induce an APPROVED verdict for modified or malicious traces.
    \item \textbf{Trace Privacy (Parameter Blinding)}: sensitive parameters and intermediate data must reach public verifiers and auditors only as salted HMAC digests.
\end{enumerate}

The gate enforces these fail-closed: any payload failing verification is rejected with exit code 1 (\texttt{BLOCKED}). Requirement 2 is where every prior system stops at the \emph{collection boundary}~\cite{apas2026agent-provenance, aas12026agent-audit}; Section~\ref{sec:witness} is our attempt to move it.

\section{Cryptographic Evidence Attestation}
EviAssure establishes verifiable release claims by packaging agent action histories into cryptographically attested evidence bundles. This section describes the Merkle tree aggregation protocol, browser Document Object Model (DOM) attestation, trace parameter blinding, and SLSA v1.0 / in-toto predicate envelopes.

\subsection{Execution Trace \& Browser UI Attestation Schema}
Each execution trace record $T_i$ captures structured action metadata across autonomous agent steps. For API and backend tool calls, $T_i$ records:
\begin{equation}
T_i = (\text{id}_i, \text{agent}_i, \text{action}_i, \text{status}_i, \text{duration}_i, H_{\text{out},i})
\end{equation}
For browser and UI agent steps, EviAssure extends attestation to capture DOM state hashes ($H_{\text{DOM}}$) and visual screenshot render digests ($H_{\text{screen}}$):
\begin{equation}
\begin{split}
T_{\text{UI},i} = (\,&\text{id}_i, \text{agent}_i, \text{action}_i, \text{url}_i,\\
&\text{elem}_i, H_{\text{DOM},i}, H_{\text{screen},i})
\end{split}
\end{equation}
$H_{\text{DOM},i} = \text{SHA256}(\text{DOM}_{\text{serialized}})$ is computed over the serialized DOM of the rendered page: the specimen serializes deterministically before hashing, so identical page states yield identical digests while any mutation --- an injected modal dialog, an altered form control --- changes the digest. Render digests $H_{\text{screen},i}$ capture canvas pixels under deterministic software rasterization --- headless Chromium, GPU and font hinting disabled, pinned viewport, immutable base image --- which eliminates the subpixel drift that would otherwise make an honest re-render fail its own digest. Each record yields a canonical leaf digest $H_i = \text{SHA256}(T_i.\texttt{to\_hash}())$ committing \emph{every} schema field --- identifier, agent, action, status, duration and output digest --- so editing any recorded field after signing, including the timing \texttt{duration\_ms}, invalidates the recomputed root and is rejected by the gate.

\subsection{Witness Protocol}\label{sec:witness}
A witness is the component that \emph{actually serves} an agent action: a tool endpoint, an API gateway, a database proxy, a sandbox syscall broker. It is not part of the agent runtime, and the security argument rests on one property: the adversary who owns the agent and the collector does not hold witness keys.

Each witness keeps a monotonic counter per session. On serving action $a$ in session $\mathit{sid}$ it returns
\begin{equation}
r = \mathrm{Sign}_{W_j}(\mathit{sid},\ \mathrm{seq},\ H_{\mathrm{act}}(a))
\end{equation}
where $H_{\mathrm{act}}$ digests the action identity the witness can see (requester, operation, returned digest) rather than the full Merkle leaf, whose timing fields are collector-side. At session end $W_j$ emits $\mathrm{Sign}_{W_j}(\mathit{sid}, n_j)$, committing to how many actions it served. \textbf{Every registered witness closes every session}, including with $n_j=0$: without that rule, dropping a witness's actions \emph{and} its closing is indistinguishable from never using the tool, which is vector O3.

The gate reconciles: authenticate every attestation under a registry-pinned witness key; require the session identifier to match; require each witness's receipts to form exactly $1..n_j$; require every receipt to bind to a submitted trace; and require every trace claiming a mediated action to carry a receipt. The bundle signature covers a digest of the whole attestation set, so a collector cannot delete receipts from a signed bundle and leave the signature intact.

\paragraph{Cost and deployment.} A receipt is one Ed25519 signature over 100 bytes at the point of service, and the gate performs $O(|T|)$ verifications. The real cost is organisational, not computational: witnesses must be deployed at the boundaries that matter, and Definition~\ref{def:wtc} makes the resulting guarantee exactly as strong as that deployment. We regard stating the guarantee relative to a declared, auditable witness set as an improvement over assuming a perfect collector, not as a weaker claim.

\paragraph{Parameter blinding.} Bundles carry digests, never raw payloads. A keyed HMAC mode (\texttt{blind\_payload}) additionally re-keys those digests per deployment, which buys unlinkability across environments and resistance to dictionary recovery of low-entropy outputs --- $\mathrm{SHA256}(\texttt{"TOKEN\_VALIDATED"})$ is recoverable by exhaustive search, the salted HMAC is not. It is deterministic, so it does not conceal repetition within one deployment. We record it as a hardening measure rather than a contribution.

\subsection{Merkle Trace Aggregation and Sparse Proof Compression}
To attest $N$ execution traces without transmitting full raw logs to every gate request, EviAssure aggregates normalized trace digests into a SHA-256 Merkle tree~\cite{merkle1989digital, laurie2014certificate}. Each trace record $T_i$ maps to leaf digest $H_i$.

For $N=1,000$ traces, transmitting raw trace logs requires \rawSizeOneKKb{}\,KB, scaling to \rawSizeOneMKb{}\,KB for $N=1,000,000$. EviAssure provides sparse Merkle proof compression, transmitting the 32-byte root $R_M$ and $O(\log N)$ inclusion proof paths $\pi_i$ for audited steps. 

Given proof path $\pi_i = \{(P_1, \text{pos}_1), \dots, (P_K, \text{pos}_K)\}$, inclusion is verified recursively by setting $C_0 = H_i$ (the committed leaf digest; leaf digests are already SHA-256 strings and are \emph{not} re-hashed at the proof boundary, matching \texttt{verify\_merkle\_proof}) and evaluating:
\begin{equation}
C_j = \begin{cases}
\text{SHA256}(C_{j-1} \parallel \text{``:''} \parallel P_j), & \text{if } \text{pos}_j = \text{right} \\
\text{SHA256}(P_j \parallel \text{``:''} \parallel C_{j-1}), & \text{if } \text{pos}_j = \text{left}
\end{cases}
\end{equation}
Leaves and internal nodes are hashed under \emph{disjoint domain prefixes} following RFC 6962~\cite{laurie2014certificate}: a leaf commits $\text{SHA256}(\texttt{0x00} \parallel T_i.\texttt{to\_hash}())$ and an internal node commits $\text{SHA256}(\texttt{0x01} \parallel L \parallel R)$ over the raw 32-byte child digests. An earlier revision of this system instead hashed both as unprefixed SHA-256 over an ASCII string with a \texttt{:} delimiter and argued that the fixed 129-byte node preimage supplied domain separation. It does not: the two input languages merely failed to overlap for the leaf schema in use, which is an accident of the schema rather than a property of the construction. Explicit prefixes make the separation structural.

The proof is valid if and only if $C_K = R_M$ \emph{and} the path length equals the committed tree depth $d=\lceil\log_2 N\rceil$ derived from the signed execution trace count. The two defences are independent and both are necessary: domain separation prevents an internal digest from ever being interpreted as a leaf, and the depth bound prevents the shortened-path substitution of the CVE-2012-2459 family, which succeeds without any hash collision. Vector V15 (Section~\ref{sec:tamper}) exercises exactly this attack against the third-party auditor path; earlier revisions of this system failed it. Transmission size for an audited step remains $<5\text{ KB}$.

\begin{lstlisting}[language=python,caption={Merkle Trace Aggregation Protocol.},label={lst:proto1}]
LEAF, NODE = b"\\x00", b"\\x01"   # RFC 6962 domain separation

def leaf_digest(content):
    return sha256(LEAF + content.encode()).hexdigest()

def node_digest(l, r):
    return sha256(NODE + bytes.fromhex(l)
                       + bytes.fromhex(r)).hexdigest()

def build_merkle_tree(leaf_digests):
    current, levels = leaf_digests, [leaf_digests]
    while len(current) > 1:
        current = [node_digest(current[i],
                     current[i+1] if i+1 < len(current)
                                  else current[i])
                   for i in range(0, len(current), 2)]
        levels.append(current)
    return current[0], levels
\end{lstlisting}

Listing~\ref{lst:proto1} implements the recursive aggregation of agent trace hashes into the final $O(\log N)$ verifiable structure.

\subsection{SLSA v1.0 and in-toto Predicate Envelopes}
To integrate with cloud-native artifact registries and software supply chain attestors, EviAssure formats each generated \texttt{EvidenceBundle} into an in-toto v0.2 Statement envelope compatible with SLSA Provenance v1.0 specifications~\cite{torres2019in-toto, slsa2023supply}. The envelope carries \texttt{\_type} \texttt{https://in-toto.io/Statement/v0.1}, the artifact digest as \texttt{subject}, \texttt{predicateType} \texttt{https://slsa.dev/provenance/v1}, and the \texttt{evidence\_id} under \texttt{predicate.buildDefinition.externalParameters}; a complete example is in the artifact.

The envelope binds the target release artifact --- an agent weight package, code container, or configuration tarball --- to the evidence attestation. \texttt{subject} carries the artifact filename and SHA-256 digest; \texttt{predicateType} asserts SLSA Provenance v1.0; within \texttt{predicate.buildDefinition}, \texttt{buildType} names the gate evaluation parameters and \texttt{externalParameters} anchors the \texttt{evidence\_id}, binding the evidence pack to the SLSA statement.

\section{Fail-Closed Policy Verification Engine}
Release authorization in EviAssure is governed by a zero-trust policy engine (\textnormal{\texttt{ReleasePolicyEngine}}) that enforces mandatory fail-closed default rules. This section details the declarative policy schema, multi-factor evaluation protocol, key governance rules, and the system-level formal security analysis.

\subsection{Declarative Policy Governance Rules}
A release policy specification $P$ defines strict quantitative safety thresholds and cryptographic verification conditions:
\begin{equation}
\begin{split}
P = (\,P_{\text{min\_pass}},\ &P_{\text{max\_drift}}, P_{\text{min\_sigs}}, P_{\text{valid\_window}},\\
&\mathcal{K}_{\text{trusted}}, \mathcal{K}_{\text{rev}}, \mathcal{A}_{\text{KMS}})
\end{split}
\end{equation}
where:
\begin{itemize}
    \item $P_{\text{min\_pass}}$: Minimum required unit and integration test pass rate (default: $100.0\%$ (100/100)).
    \item $P_{\text{max\_drift}}$: Maximum allowable unresolved security drift findings (default: $0$).
    \item $P_{\text{min\_sigs}}$: Required threshold number of valid signatures from authorized keys (default: $K=1$).
    \item $P_{\text{valid\_window}}$: Evidence timestamp freshness validity window $\Delta T_{\text{max}} = 3,600\text{ s}$ with future clock-skew tolerance $\delta_{\text{skew}} = 30\text{ s}$ (assuming RFC 5905 NTP synchronisation).
    \item $\mathcal{K}_{\text{trusted}}$: Trusted key registry pinning authorized key IDs to Ed25519 public keys (\nolinkurl{governance/trusted_keys.yaml}). Verification uses the pinned key, never one supplied inside a bundle; unregistered IDs are rejected. Authority thus rests on a small, versioned, offline-pinnable root set, following key transparency~\cite{melara2015coniks} and delegation hierarchies~\cite{kuppusamy2016tuf}.
    \item $\mathcal{K}_{\text{rev}}$: Key Revocation List (CRL) tracking compromised or deprecated key identifiers.
    \item $\mathcal{A}_{\text{KMS}}$: Required Cloud KMS or HSM key ARN boundary pattern matching.
\end{itemize}

Listing~\ref{lst:proto2} outlines the complete zero-trust verification algorithm enforced by \textnormal{\texttt{ReleasePolicyEngine}}.

\begin{lstlisting}[language=python,caption={Fail-Closed Policy Gate Verification Protocol.},label={lst:proto2}]
# In: Evidence E, Policy P, revoked K_rev, seen N_seen
# Out: D in {APPROVED, BLOCKED}; any exception => BLOCKED
def evaluate_release_gate(E, P, K_rev, N_seen):
    if N_seen is None: return BLOCKED  # no replay state
    valid = set()
    for s in E.signatures:
        if s.key_id in K_rev: continue
        if s.key_id not in K_trusted: continue   # registry
        pk = K_trusted[s.key_id]                 # pinned only:
        if s.pub_key and s.pub_key != pk: continue
        if not match_kms_arn(s.kms_arn, P.kms): continue
        if verify_ed25519(canon(E), s.sig, pk):
            valid.add(s.key_id)          # distinct signers
    if len(valid) < P.min_signatures:     return BLOCKED
    if merkle_root(E.traces) != E.merkle_root: return BLOCKED
    if E.traces_count != len(E.traces):   return BLOCKED
    if E.test_pass_pct < P.min_pass_pct:  return BLOCKED
    if E.unresolved_drift > P.max_drift:  return BLOCKED
    if E.nonce in N_seen:                 return BLOCKED
    if not fresh(E.timestamp, P.window, skew=30): return BLOCKED
    N_seen.add(E.nonce);  return APPROVED
\end{lstlisting}

Listing~\ref{lst:proto2} outlines the zero-trust policy engine logic that strictly enforces fail-closed behavior on signature or trace discrepancies.

\subsection{Formal Security Analysis}\label{sec:proofs}

The analysis has two layers. Theorem~\ref{thm:failclosed} is primitive-level: a game sequence reducing to EUF-CMA and collision resistance. Theorem~\ref{thm:authorization} is system-level: what an APPROVED verdict \emph{means}, and the trust boundaries --- collector, key registry, nonce state --- that meaning rests on.

\begin{theorem}[Fail-Closed Release Soundness]\label{thm:failclosed}

Under the assumption that Ed25519~\cite{bernstein2012high} is EUF-CMA secure~\cite{goldwasser1988digital, brendel2021ed25519} and SHA-256 is collision-resistant, no probabilistic polynomial-time adversary $\mathcal{A}$ can induce \textnormal{\texttt{ReleasePolicyEngine}} to output $\text{APPROVED}$ for a modified trace history, forged signature, replayed nonce, or non-compliant evidence payload except with negligible probability $\text{negl}(\lambda)$.
\end{theorem}

\begin{proof}
Two hops carry content. $\mathbf{Game}_1$ aborts if $\mathcal{A}$ produces a valid signature over a payload the honest packager never issued; an EUF-CMA adversary $\mathcal{B}$ installs its challenge key as the sole registry entry and answers packaging requests through its signing oracle on $\mathrm{canon}(E)$, so the simulation is perfect and a $\text{Game}_1$ abort is a forgery on a never-queried message. $\mathbf{Game}_2$ aborts on $\mathcal{T}^*\neq\mathcal{T}$ with equal root; under domain-separated leaves and the committed depth this needs a SHA-256 collision. Replay, freshness, revocation, registry membership and threshold comparison are table lookups over the signed payload, rejecting with probability $1$ given correct gate state, and contribute no term. Hence $\mathrm{Adv}^{\text{tamper}} \le \mathrm{Adv}^{euf\text{-}cma}_{\mathrm{Ed25519}}(\mathcal{B},t,q_s) + \mathrm{Adv}^{coll}_{\mathrm{SHA256}}(\mathcal{C},t)$, read at the 128-bit level rather than asymptotically in $\lambda$, since both primitives are fixed-parameter.
\end{proof}

Theorem~\ref{thm:failclosed} bounds an adversary's ability to \emph{fake} evidence; it says nothing on its own about what a genuine, honestly-signed evidence pack entitles its holder to. That is the release-gate question, and it is Theorem~\ref{thm:authorization}.

\begin{theorem}[Evidence-Bound Release Authorization Soundness]\label{thm:authorization}
Fix a policy version $P$ and a trusted key registry $\mathcal{K}_{\text{trusted}}$. If \textnormal{\texttt{ReleasePolicyEngine}} outputs $\text{APPROVED}$ for evidence $E$, then, except with probability $\text{negl}(\lambda)$, all of the following hold:
\begin{enumerate}
    \item[(i)] \textbf{Authenticity:} $E$ carries a valid Ed25519 signature by a non-revoked key pinned in $\mathcal{K}_{\text{trusted}}$; attacker-generated keys, spoofed key IDs, and revoked keys cannot produce a valid signature.
    \item[(ii)] \textbf{Evidence binding:} the recomputed Merkle root over the submitted trace set equals the signed root, the signed trace count equals the number of submitted traces, and every inclusion proof binds to the committed tree depth under disjoint leaf/node domains. The trace set the gate evaluated is therefore \emph{the one the collector signed} --- no trace was added, removed, reordered or edited between collection and adjudication. This says nothing about the relation between the signed trace set and the underlying execution; that gap is Definition~\ref{def:completeness} and is not closed by any cryptographic check.
    \item[(iii)] \textbf{Freshness:} the timestamp lies within the validity window $\Delta T_{\text{max}}$ and the skew bound $\delta_{\text{skew}}$, and the nonce has not been processed within the window; the release is not a replay of an earlier approved bundle.
    \item[(iv)] \textbf{Policy compliance:} all declared thresholds (test pass rate, unresolved drift, KMS ARN boundary, signature threshold $K$) satisfy policy version $P$; an APPROVED verdict under a different policy version is not an APPROVED verdict for this release.
\end{enumerate}
\end{theorem}

\paragraph{Scope and Operational Assumptions.}
The guarantees hold conditionally on (a) the \emph{collector assumption} of Definition~\ref{def:completeness} --- the evidence collector observed and recorded every action of the release execution --- which supplies the completeness half of the guarantee, and (b) the \emph{key-management assumption} --- registry keys are honestly generated and the HSM/KMS enforces the ARN boundary. The theorem asserts nothing about actions that never reached the collector, nor about the semantic correctness of recorded trace content; both are defined in the threat model and handled by the out-of-band semantic inspection layer.

\begin{proof}
\textbf{(i)} Follows from Theorem~\ref{thm:failclosed}, Game$_1$, plus the deterministic registry checks enforced before any signature verification (\texttt{policy.py}): unknown key IDs, bundle-supplied keys disagreeing with the registry, and revoked keys are rejected outright, so per-signature success is bounded by $\text{Adv}_{\text{Ed25519}}^{\text{euf-cma}}(\mathcal{A})$. \textbf{(ii)} Follows from Theorem~\ref{thm:failclosed}, Game$_2$, plus the deterministic signed-count check $N = |\text{traces}|$; substitution requires either a hash collision or a broken signature. \textbf{(iii)} Freshness, skew, and nonce checks are deterministic (Game$_3$; Listing~\ref{lst:proto2}) and hold with probability $1$ given correct gate state, with the disclosed caveat that replay protection requires sharing the nonce cache across gate replicas in distributed environments. \textbf{(iv)} Threshold comparisons are deterministic over the signed payload and the pinned policy version. Composition yields the statement; the only probabilistic terms reduce to the primitive advantages of Theorem~\ref{thm:failclosed}.
\end{proof}

\section{Deployment Integration and Workload Specimens}
EviAssure runs as a GitHub Action against two synthetic workload specimens: a backend multi-step agent (\nolinkurl{specimens/agent_runner.py}) exercising JWT claims, vector retrieval and sandboxed SQL, and a browser agent (\nolinkurl{specimens/web_app_runner.py}) exercising navigation, clicks, DOM-state hashing and screenshot digests. Both are simulations. A third specimen (\nolinkurl{specimens/live_agent_runner.py}) is not: it drives a real tool-calling LLM whose tools are fronted by witnesses, and Section~\ref{sec:live} reports what that produced. The production collector of Section~\ref{sec:system_threat} (eBPF syscall mediation) remains an architectural proposal rather than an evaluated component --- which is precisely why Section~\ref{sec:witness} moves the completeness guarantee onto witnesses that a compromised collector cannot speak for.

\subsection{CI/CD Integration}
The gate ships as a GitHub Action that loads the pinned key and witness registries and the policy, ingests the \texttt{EvidenceBundle}, runs \texttt{ReleasePolicyEngine} (\nolinkurl{scripts/verify_release_gate.py}), and on failure emits structured diagnostics and exits non-zero on every output path.

\subsection{Agent Execution Trace Corpus (\texorpdfstring{$N=\corpusProfiles$}{N=1,075})}\label{sec:corpus}
The corpus (\nolinkurl{corpus/agent_trace_corpus.json}) contains \corpusProfiles{} profiles / \corpusRecords{} trace records across five agent architectures, each profile carrying four to six steps: \corpusClean{} clean, \corpusOvert{} \emph{overt} anomalies over ten classes, and \corpusStealth{} \emph{stealth} anomalies over five classes. It is entirely synthetic and templated; this is stated in the artifact's own \texttt{provenance} field and is the principal threat to external validity (Section~\ref{sec:limitations}).

The overt/stealth split exists because an earlier revision of this corpus was not a benchmark. Every planted anomaly used an action name and status absent from clean traffic, and the detector keyed off a hard-coded list of exactly those names: generator and detector were one list, written together, so the reported recall was a property of the generator. \emph{Overt} classes retain that structure (out-of-vocabulary action and status) and are the easy half. \emph{Stealth} classes are the point: a mutating statement issued through the read-only query path, a ledger transfer to an attacker-controlled destination, retrieval returning injected context, extraction used as an exfiltration channel, a linter invoked with findings suppressed. Each uses an action, a status and a duration drawn from clean traffic, so no per-leaf vocabulary or envelope test can separate them. Section~\ref{sec:twolayer} reports both halves separately.
\subsection{Forensics and Diagnostics: CLI Inspection \& Audit Tool}\label{sec:forensics}
To support incident response after a release has already occurred, EviAssure ships an offline forensic inspection utility (\nolinkurl{scripts/audit_trace_forensics.py}). It parses serialized \texttt{EvidenceBundle} files, reconstructs the full Merkle tree, verifies Ed25519 signatures against the pinned registry and revocation list, and isolates leaf-index anomalies. Presented with a tampered payload it reports the exact leaf position $i$ and the output-hash mismatch, emitting structured JSON for security operations centre (SOC) ingestion.

\section{Systems Security Evaluation}\label{sec:eval}
The empirical evaluation follows a deliberate two-part strategy: a micro-level validation of the gate's enforcement mechanisms, and a macro-level evaluation of the system deployed at corpus scale.

\textbf{Mechanism evaluation.} Baseline performance is measured through Merkle scaling latency and multi-core verifier throughput. Adversarial resilience is measured against the vector suite: twelve vectors derived from the five capability classes ($\mathcal{A}_1$--$\mathcal{A}_5$), each exercising one enforcement, plus five authored against the design rather than against the check list --- the ones with the power to fail, and three of which did. Clean negative controls measure the false-block rate, without which a block rate is uninterpretable.

\textbf{System-level deployment.} A corpus-scale evaluation over \corpusProfiles{} agent trace profiles exercises the interaction between the cryptographic integrity gate and an out-of-band semantic inspection layer --- and, importantly, shows where that interaction still leaves a gap.

Results are split by kind. \emph{Deterministic} security results (block counts, ablation, inspection) are produced by \nolinkurl{scripts/run_security_eval.py} into \nolinkurl{results/security_evaluation.json} and are platform-independent: they reproduce identically on any machine. \emph{Timed} measurements are wall-clock timings recorded by \nolinkurl{scripts/run_release_benchmark.py} into \nolinkurl{results/benchmark_summary.json} on an Apple M4 Max with 14 cores (10 performance, 4 efficiency; no SMT, so logical and physical core counts coincide) running Python 3.14.x. Each scaling point, throughput configuration, proof cost, blinding cost, and hashing figure is the mean over \benchRepeats{} runs, with the standard deviation recorded alongside in the artifact (\texttt{*\_std} fields); per-worker throughput aggregates 1,000 policy evaluations per run. Tamper vectors and gate verdicts are deterministic checks evaluated once each, so run-to-run dispersion there is zero by construction; sampling uncertainty over the finite vector set is reported instead, as a Wilson 95\% interval on every rate.

\subsection{Merkle Scaling up to \texorpdfstring{$N=1,000,000$}{N=1,000,000} Traces}
Merkle build time was evaluated from $N=10$ to $N=10^6$ over \benchRepeats{} runs per point. Construction scales to $10^6$ traces in $\merkleBuildOneMMs\text{ ms}$ (mean; $\sigma=\merkleBuildOneMStd\text{ ms}$ across runs), and to $\merkleBuildMs\text{ ms}$ for $N=100,000$. We previously reported a ``packaging overhead'' percentage here and have withdrawn it. The numerator timed only post-tree bundle construction and signing --- a constant, independent of $N$ --- while the denominator grew as $1.5\text{ ms} \times N$, so the ratio fell monotonically for arithmetic reasons rather than measuring any property of the system. Merkle construction, the component that actually scales, is reported directly above.


\subsection{Parallel Verifier Throughput}
Verifier throughput was evaluated using a Python \texttt{ProcessPoolExecutor} across 1, 2, 4, 8, and 16 worker cores over 1,000 evaluation requests per run and \benchRepeats{} runs per configuration; each evaluation is a distinct fresh-nonce bundle against a warm policy engine (evidence creation and replay-state checks are measured separately, so the timed region is steady-state verification). Throughput rises from \singleWorkerThroughput{} ops/s on a single worker to a peak of \peakThroughput{} $\pm$ \peakThroughputStd{} operations/second (mean $\pm$ std) at \peakWorkers{} workers (a $\peakRatio\times$ speedup), then declines. We do not attribute the decline to a hardware effect: each task is a three-trace bundle shipped to a worker process, so the measurement is dominated by inter-process serialization rather than verification. Two consequences must be read together with Section~\ref{sec:eval}: this figure characterises \emph{small} bundles, and because the gate recomputes the whole tree (Listing~\ref{lst:proto2}, step 2), per-bundle verification cost is linear in $N$ --- at $N=10^6$ the gate is bounded by the $\merkleBuildOneMMs\text{ ms}$ rebuild, not by this throughput number.


\subsection{Sparse Inclusion Proofs}
Auditors verifying individual high-risk steps should not re-process full logs (\rawSizeOneMKb{}\,KB at $N=10^6$). At $N=10^6$ the committed depth is $d=\lceil\log_2 N\rceil=20$; proof generation takes $\sparseProofGenMs\text{ ms}$, yielding a \sparseProofSizeKb{}\,KB payload of \sparseProofNodes{} node digests that verifies in $\sparseProofVerifyMs\text{ ms}$ --- a bandwidth reduction of roughly $99.999\%$.

\subsection{Adversarial Vector Suite}\label{sec:tamper}
\begin{table}[t]
\centering
\caption{Adversarial vectors and gate outcomes. V1--V12 target one enforcement each; V14--V18 were authored against the design, independently of the check list. V13 is retained as a configuration check but is no longer scored (Section~\ref{sec:limitations}).}
\label{tab:tamper}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lp{2.9cm}ll@{}}
\toprule
\textbf{ID} & \textbf{Vector} & \textbf{Class} & \textbf{Outcome} \\
\midrule
\multicolumn{4}{@{}l}{\emph{Mechanism vectors (one per enforcement)}}\\
V1 & Unsigned payload & $\mathcal{A}_2$ & Blocked \\
V2 & Tampered trace digest & $\mathcal{A}_1$ & Blocked \\
V3 & Attacker-key signature & $\mathcal{A}_2$ & Blocked \\
V4 & Replayed nonce (same gate) & $\mathcal{A}_3$ & Blocked \\
V5 & Expired timestamp & $\mathcal{A}_3$ & Blocked \\
V6 & Unresolved drift & policy & Blocked \\
V7 & Sub-threshold pass rate & policy & Blocked \\
V8 & Forged Merkle root & $\mathcal{A}_1$ & Blocked \\
V9 & Revoked key ID & $\mathcal{A}_4$ & Blocked \\
V10 & Duplicate-key wire doc & $\mathcal{A}_5$ & Blocked \\
V11 & Future clock skew & $\mathcal{A}_3$ & Blocked \\
V12 & Trace-set truncation & $\mathcal{A}_1$ & Blocked \\
\midrule
\multicolumn{4}{@{}l}{\emph{Independent vectors (authored against the design)}}\\
V14 & Attacker key, registry off & $\mathcal{A}_2$ & Blocked$^\dagger$ \\
V15 & Internal node as leaf & $\mathcal{A}_1$ & Blocked$^\dagger$ \\
V16 & Cross-replica nonce replay & $\mathcal{A}_3$ & \textbf{Not blocked} \\
V17 & 64-char leaf confusion & $\mathcal{A}_1$ & Blocked$^\dagger$ \\
V18 & Post-signing DOM mutation & $\mathcal{A}_1$ & Blocked \\
\midrule
\multicolumn{2}{@{}l}{Scored total} & \multicolumn{2}{l@{}}{\eviBlocked/\eviTotal\ (\eviPct\%)}\\
\multicolumn{2}{@{}l}{Clean negative controls} & \multicolumn{2}{l@{}}{\falseBlockK/\falseBlockN\ falsely blocked}\\
\bottomrule
\end{tabular}
\end{table}

EviAssure blocks \eviBlocked{} of \eviTotal{} scored vectors (\eviPct\%, 95\% CI [\eviCIlo, \eviCIhi]), and falsely blocks \falseBlockK{} of \falseBlockN{} clean negative controls. Three points about how that number should be read.

\textbf{The mechanism vectors do not demonstrate resilience.} V1--V12 map close to one-to-one onto the gate's checks, because we wrote both; they are a completeness test for the implementation and nothing more. An earlier version argued this 1:1 map proved complete coverage; V14 and V17 are why that is invalid.

\textbf{Three vectors ($\dagger$) broke earlier revisions.} V14 exploits a disabled trusted-key registry: the gate verified against the key carried \emph{inside the bundle}, so an adversary with their own keypair and a conformant ARN was APPROVED with zero violations. V17 exploits a \texttt{len(x) != 64} heuristic, letting a 64-character leaf string be adopted as its own digest. V15 is the CVE-2012-2459-family internal-node substitution against the auditor path. None was reachable by the mechanism suite, which is the argument for authoring vectors against the design.

\textbf{V16 is not blocked, and we report it.} The same approved bundle presented to a second gate replica that does not share nonce state is APPROVED, because replay protection is a property of gate \emph{state}, not of the evidence. The shipped verifier persists nonces and fails closed when the ledger is unavailable, but cross-replica correctness needs shared storage the prototype does not provide.

\textbf{Differential wire fuzzing.} The previous campaign reported 1,000/1,000 blocked and carried no information: every mutation perturbed a field inside the signed payload, so a verifying bundle was unreachable by construction, and it contained no clean cases. The replacement perturbs the \emph{serialization} of a validly signed bundle --- key order, insignificant whitespace, unicode escaping, number formatting, duplicate keys, byte-order marks --- where both outcomes are reachable. Over \fuzzCases{} cases (20\% clean controls): \fuzzBlockedK/\fuzzBlockedN{} semantics-changing encodings blocked, \fuzzApprovedK/\fuzzApprovedN{} semantics-preserving encodings approved. This campaign found a real defect: the duplicate-key detector accumulated key counts across every object in the document, so sibling objects in the \texttt{traces} array registered as duplicates and any deployment actually supplying the raw wire text rejected every multi-trace bundle. It is fixed and regression-tested.

\subsection{Per-Check Ablation}
Each enforcement was disabled in turn and the suite re-evaluated (matrix in the artifact's \texttt{ablation} block). Disabling the signature check releases V1, V3, V9, V14 and V18; removing Merkle re-derivation releases V2 and V8; the quality, drift, freshness and replay checks each release exactly the vectors they target (V7; V6; V5 and V11; V4). V16 escapes every variant, including the full gate.

Two results are worth stating precisely, because the previous version of this section stated one of them incorrectly.

First, \textbf{not every check is necessary against this suite}. Disabling the signed trace-count binding releases no vector: V12 is independently caught by root recomputation. The count binding earns its place by protecting the \emph{auditor} path --- proof depth is derived from the signed count --- not by adding coverage at the gate. We previously wrote that ``every check is necessary''; the artifact's own matrix contradicted it.

Second, \textbf{emptying the trusted-key registry collapses the gate rather than weakening it}: all Ed25519 evidence is rejected because no signature can be authenticated. This is now true of the implementation. When we first claimed it, the ablation flag redirected verification to the bundle-supplied key rather than disabling it --- the V14 bypass --- so establishing the claim required fixing the code, not re-reading the matrix.

\subsection{Omission Attacks}\label{sec:omission}
The tamper suite and the omission suite test different things. A tamper vector modifies evidence and is caught by an integrity check. An omission vector modifies \emph{nothing}: the adversary performs an action and declines to record it, then signs honestly over what remains. There is no discrepancy to detect, which is why Theorem~\ref{thm:separation} rules the class out for document verifiers rather than merely predicting they will do badly at it.

We construct six (Table~\ref{tab:omission}) plus an honest fully-witnessed control that every system must approve --- without which a reconciler that rejects everything would score perfectly.

\begin{table}[t]
\centering
\caption{Omission attacks detected, out of \omVectors{}. All systems approve the honest control (OC1). Baselines are executed, not modelled.}
\label{tab:omission}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lp{3.6cm}c@{}}
\toprule
& \textbf{Verification approach} & \textbf{Detected} \\
\midrule
in-toto/DSSE & signature over the document & \omDsse/\omVectors \\
TUF & expiry, thresholds, revocation & \omTuf/\omVectors \\
AAS-1 style & per-issuer hash chaining & \omChain/\omVectors \\
Receipts (2026) & per-action receiver attestation & \omReceipts/\omVectors \\
EviAssure & full gate, no reconciliation & \omNoWtc/\omVectors \\
\textbf{EviAssure} & \textbf{+ witnessed completeness} & \textbf{\omWtc/\omVectors} \\
\bottomrule
\end{tabular}
\end{table}

Three results are worth reading carefully. First, \textbf{EviAssure without reconciliation detects \omNoWtc{} of \omVectors{}} despite enforcing signatures, Merkle re-derivation, count binding, freshness and replay. The mechanism, not some incidental check, is what closes the class.

Second, \textbf{per-action receipts detect \omReceipts{} of \omVectors{}}. Receipts catch the two attacks that leave a trace \emph{without} an attestation --- cross-session splicing and fabrication --- and miss all four truncations, because dropping an action drops its receipt and the remaining pairs still match. This is the empirical form of the paper's central observation: a receipt proves an action happened; a set of receipts does not prove no other action happened.

Third, \textbf{hash chaining detects \omChain{} of \omVectors{}}. A chain links records that are present, so an interior gap breaks it and a suffix truncation does not --- the end simply moves. This is the precise limitation of AAS-1's omission story, and it is why the closing count, not the chain, is what makes truncation detectable.

\subsection{Comparative Gate Analysis}\label{sec:comparative}
The previous version of this comparison was not defensible. It scored strawman baselines --- a ``Sigstore/Cosign-style'' gate that was a presence check on a JSON field, an ``exit-code gate'' that was \texttt{test\_pass\_pct > 0} --- against a vector set derived from our own check list, and the resulting near-zero figures reached the abstract as though they characterised the state of the art. They did not. We have rebuilt it.

Each baseline is now configured as a competent engineer would deploy it, and executed:
\begin{enumerate}
    \item \textbf{in-toto/DSSE verification} (executed, \texttt{securesystemslib} DSSE PAE + Ed25519): authenticates the envelope against a \emph{pinned trust root} rather than a key travelling with the payload. Blocks \dsseBlocked/\dsseTotal{} (\dssePct\%, CI [\dsseCIlo, \dsseCIhi]).
    \item \textbf{TUF metadata verification} (executed, \texttt{python-tuf}): real Root/Timestamp metadata with \texttt{expires}, signature thresholds and root-driven revocation. Blocks \tufBlocked/\tufTotal{} (\tufPct\%, CI [\tufCIlo, \tufCIhi]).
    \item \textbf{OPA Rego policy} (executed when the \texttt{opa} binary is present; \nolinkurl{governance/baseline_opa.rego}): \opaBlocked/\opaTotal{} (\opaPct\%).
    \item \textbf{Composed pipeline} (DSSE $\wedge$ TUF $\wedge$ OPA): \compBlocked/\compTotal{} (\compPct\%, CI [\compCIlo, \compCIhi]).
    \item \textbf{Unauthenticated status gate}: \cigateBlocked/\cigateTotal{}. Retained explicitly as a \emph{floor}, not a competitor.
\end{enumerate}

The honest headline is therefore \eviBlocked/\eviTotal{} versus \compBlocked/\compTotal{}, and the intervals overlap: with \eviTotal{} vectors this evaluation cannot support a strong quantitative separation, and we do not claim one. What it does support is a \emph{qualitative} account of which vectors survive a modern composed pipeline and why. The composed baseline blocks every authentication, freshness, revocation and policy-threshold vector; TUF alone blocks \tufBlocked{} (Section~\ref{sec:related-2026}).

The vectors it does not block are the ones this paper is actually about: V2 and V8 (trace-level integrity --- no baseline recomputes a Merkle root over an action history), V12 (trace-set truncation), V15 and V17 (proof-path and leaf-typing attacks on the audit surface), and V4 (within-gate replay, which requires the verifier to hold state across submissions rather than to validate a document). That is the delta: \emph{binding the release decision to the execution trace, and holding state while deciding}. It is a narrower claim than 100\% versus 30.8\%, and it is one the evaluation can carry.

\subsection{Corpus-Scale Layered Evaluation}\label{sec:twolayer}
Neither the tamper nor the omission suite reaches the semantic axis: a complete, witnessed, correctly signed bundle can still record behaviour nobody authorised. The corpus measures that third layer (Table~\ref{tab:corpus}).

\begin{table}[t]
\centering
\caption{Layered evaluation over \corpusProfiles{} profiles / \corpusRecords{} trace records. L3 uses a detector fitted on clean profiles only.}
\label{tab:corpus}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lp{3.1cm}cc@{}}
\toprule
\textbf{Layer} & \textbf{Check} & \textbf{Result} & \textbf{Rate} \\
\midrule
L1 Integrity & Merkle root consistency & \corpusProfiles/\corpusProfiles & 100\% \\
L1 Integrity & Depth-bound proofs valid & \corpusProfiles/\corpusProfiles & 100\% \\
L2 Gate & Clean APPROVED & \corpusClean/\corpusClean & 100\% \\
L2 Gate & Anomalous APPROVED & \insRecallN/\insRecallN & 100\% \\
L3 Inspect & Overt recall & \insOvertK/\insOvertN & 100\% \\
L3 Inspect & \textbf{Stealth recall} & \textbf{\insStealthK/\insStealthN} & \textbf{0\%} \\
L3 Inspect & Overall recall & \insRecallK/\insRecallN & \insRecallPct\% \\
L3 Inspect & Clean falsely flagged & \insFPK/\insFPN & 0\% \\
\bottomrule
\end{tabular}
\end{table}

L1 and L2 are as before: all bundles are Merkle-consistent with valid depth-bound proofs, all clean profiles are APPROVED, and \emph{all} anomalous profiles are also APPROVED. We report the latter rather than hiding it --- semantic misbehaviour that is cryptographically well-formed is outside the gate's scope by design.

L3 is the layer whose evaluation we rebuilt. The detector no longer consults any list of anomalous actions. It is fitted on clean profiles alone, learning an action vocabulary, a status vocabulary and a per-action duration envelope, and every anomaly class is held out from that fit. Under this protocol it recalls \insOvertK/\insOvertN{} overt anomalies and \insStealthK/\insStealthN{} stealth anomalies --- overall \insRecallK/\insRecallN{} (\insRecallPct\%, CI [\insRecallCIlo, \insRecallCIhi]) at \insFPK/\insFPN{} false positives.

The zero is the result. Per-leaf inspection detects agents that do things they have never done before; it is blind to an agent doing a permitted thing for an impermissible reason, which is precisely what a prompt-injected agent looks like in a trace. Combined with L2, the layered picture is: the gate proves what was recorded is genuine, inspection catches structural novelty, and \emph{neither} catches semantically disguised misbehaviour. The earlier 50/50 recall reported here was a property of that shared list (Section~\ref{sec:corpus}), not a measurement.

\subsection{Live Agent Sessions}\label{sec:live}
Everything above is computed over a templated corpus, which leaves the paper's
own premise untested: does witnessed completeness survive an agent that actually
decides what to do? We therefore ran \liveSessions{} sessions of a real
tool-calling LLM (\texttt{\liveModel}) whose tools are fronted by the witnesses
of Section~\ref{sec:witness}. The model chooses the actions; nothing is scripted.

The sequences are genuinely non-deterministic --- \liveDistinctSeq{} of
\liveSessions{} sessions were distinct (\liveNondetPct\%), over
\liveActionsMean{} $\pm$ \liveActionsStd{} actions --- and the agent generated
its own failures, malformed SQL refused by the read-only path (\texttt{DENIED})
and queries against non-existent tables (\texttt{ERROR}), rather than executing
a happy path we wrote for it.

\textbf{Reconciliation is sound on real traces}: the honest control reconciles
\liveControlOk{}, and re-deriving the omission attacks per session detects
\liveOmDetected/\liveOmTotal. Detection is cryptographic, so this confirms rather than
surprises --- but a protocol that failed here would have been refuted.

\textbf{Witness coverage was \liveCoverageMean\% on average, \liveCoverageMin\%
at worst.} This is the quantity Definition~\ref{def:wtc} leaves open: the
guarantee holds relative to the witness set, and that set covered most but not
all of what a real agent did. The residue was \texttt{\liveUnmediated}, a local
bookkeeping call no witness fronts by construction. That is the honest shape of
the property --- completeness over the actions a deployment chose to mediate,
with a measurable remainder rather than a hypothetical one.
\liveFailures{} of \liveRequested{} sessions was lost to a provider-side
output-parsing error, which we report rather than retry into silence.

\subsection{Limitations and Threats to Validity}\label{sec:limitations}
\textbf{The corpus is still synthetic.} Section~\ref{sec:live} runs a real tool-calling LLM against the witness protocol, so the completeness claim is no longer evaluated only on templates. The \corpusProfiles-profile corpus behind Sections~\ref{sec:twolayer} and the semantic-inspection results is not: it remains synthetic and templated,, as the artifact's own \texttt{provenance} field declares. The layer this paper contributes is now exercised on non-deterministic behaviour; the layer it inherits is not. The live sessions are also modest in scale (\liveSessions{}), single-provider, and use benign tools; they establish that the protocol composes with a real agent and what coverage it achieved, not how it behaves under an adversarial one.

\textbf{The collector is unimplemented.} Every security guarantee is conditioned on complete mediation at the collection boundary (Definition~\ref{def:completeness}). The production design grounds this at the OS boundary via eBPF; the evaluation uses Python instrumentation wrappers. The load-bearing component is therefore an architectural proposal, not a measured one.

\textbf{The gate does not block semantic misbehaviour, and inspection misses the hard half.} L2 approves all \insRecallN{} anomalous profiles by design; held-out L3 inspection recalls \insStealthK/\insStealthN{} stealth anomalies (Section~\ref{sec:twolayer}). Content-level policy over trace semantics is future work.

\textbf{V16 is unblocked.} Cross-replica replay protection requires shared nonce storage the prototype does not implement (Section~\ref{sec:tamper}).

\textbf{Witness trust and availability.} Witnessed completeness is exactly as strong as the declared witness set. An adversary holding a witness key mints valid receipts and closings for that witness, and action types no witness mediates lie outside the guarantee by construction (Definition~\ref{def:wtc}). We evaluate neither witness compromise nor key rotation, and a witness that fails to close a session blocks the release --- fail-closed, but a denial-of-service surface a deployment must plan for.

\textbf{Vector-set construct validity.} V1--V12 were authored alongside the checks they exercise and demonstrate only that those checks fire. V14--V18 were authored against the design and three of them broke earlier revisions, which is evidence that the suite has some power --- but a \eviTotal{}-vector suite gives wide intervals (CI [\eviCIlo, \eviCIhi]) and no suite authored by the system's designers can establish resilience to novel attack.

\textbf{Measurement and key management.} Timed figures are means over \benchRepeats{} runs on one platform; $\sigma$ captures within-run noise only. The artifact signs with a static demo key so results reproduce; there is no real KMS round trip, and the 15--25\,ms figure is an estimate.


\section{Related Work}

\textbf{Positioning.} We state the boundary of the claim first: threshold signing, metadata expiry, rollback protection and key revocation are \emph{not} novel here --- TUF has supplied them since 2010~\cite{samuel2010surviving}, and our executed TUF baseline blocks \tufBlocked{} of \tufTotal{} vectors on that basis alone (Section~\ref{sec:comparative}). EviAssure bridges a narrower gap than earlier drafts of this paper claimed: it binds the release decision to a Merkle-attested \emph{execution trace} and holds replay state across submissions, which is what none of the systems below does.

\subsection{Supply Chain Provenance}
in-toto~\cite{torres2019in-toto}, SLSA~\cite{slsa2023supply} and Sigstore/Cosign~\cite{sigstore2022cosign} provide cryptographic provenance for build pipelines and image digests, including vetted runtime-trace predicates~\cite{intoto_runtime_trace}; any release pipeline should use them for the static artifact side. What they do not provide is the stateful authorization layer of Section~\ref{sec:proofs}. EviAssure supplies it while remaining envelope-compatible --- an \texttt{EvidenceBundle} is an in-toto Statement and can carry SLSA provenance, \texttt{agent-decision} records, or custom predicates alongside Merkle-attested traces.

\subsection{Policy Engines and Workload Identity}
Policy engines (OPA~\cite{opa2021openpolicy}, Kyverno~\cite{kyverno2022policy}) evaluate declarative rules over admission payloads but compute no Merkle root over an action history and hold no replay state. Workload identity frameworks (SPIFFE/SPIRE~\cite{spiffe2020spire}, Macaroons~\cite{birgisson2014macaroons}) are orthogonal: they establish \emph{who} the runner is, not what action history it produced. Runtime LLM guardrails filter inference-time inputs and outputs; they do not gate the promotion of a checkpoint, prompt template, or tool scope. Update frameworks are the closest prior art on governance: TUF~\cite{samuel2010surviving} and its delegation extensions~\cite{kuppusamy2016tuf, moore2023artemis} supply expiry, thresholds, rollback and revocation, which is why we execute TUF as a baseline rather than claim those properties.

\subsection{AI-Agent Provenance Standards}\label{sec:related-2026}
Three 2026 initiatives address agent provenance as a \emph{record format}. The in-toto \texttt{agent-decision/v0.1} RFC~\cite{intoto2026agent-decision} standardises the shape of a signed policy-decision record, leaving freshness, replay and revocation to the verifier. APAS~\cite{apas2026agent-provenance} builds provenance chains on in-toto Statements and grades itself candidly: at its lower levels provenance is ``self-attested --- an attacker who compromises the orchestrator can forge records.'' AAS-1~\cite{aas12026agent-audit} defines evidentiary record classes with per-issuer hash chains that detect omission \emph{between} records a verifier already holds --- which, as Section~\ref{sec:omission} measures, a suffix truncation evades.

Theorem~\ref{thm:separation} explains why this is structural rather than a maturity gap: each verifies a submitted document, and no such verifier can achieve replay-freedom or witnessed completeness. Their records ride inside EviAssure's envelope unmodified; what EviAssure adds is the state and the second trust domain that a document cannot carry.

\section{Ethics Considerations}
All datasets, trace profiles and harnesses are synthetic and locally generated: no human subjects, no customer PII, no live credentials, and no measurement touching third-party systems. Dual-use considerations --- workplace surveillance, parameter blinding, and what this system does \emph{not} prevent --- are analysed in Appendix~\ref{sec:appendix_ethics}.

\section{Conclusion}
EviAssure binds a release decision to a domain-separated, depth-bound Merkle attestation of an agent's execution trace, evaluated by a stateful fail-closed gate. Its central claim is Witnessed Trace Completeness: reconciling collector evidence against signed witness receipts and closing counts detects \omWtc/\omVectors{} omission attacks every executed record-format baseline misses, and Theorem~\ref{thm:separation} shows no stateless document verifier can close that class at all. On the tamper axis it blocks \eviBlocked/\eviTotal{} against \compBlocked{} for an executed in-toto/DSSE + TUF + OPA composition, and fails one --- cross-replica nonce replay --- which we report. Held-out inspection recalls \insOvertK/\insOvertN{} overt and \insStealthK/\insStealthN{} disguised anomalies: the honest boundary of the contribution. Evidence-bound release assurance makes an unauthorised agent release undeniable and attributable, not impossible.

\ifanonymous\else
\section*{Acknowledgements}
P. Radanliev and C. Maple are researchers at The Alan Turing Institute.
\fi

\bibliographystyle{plain}
\bibliography{references}

\appendix
\section*{Use of Generative AI and Automated Tooling}
The authors disclose the use of automated AI coding assistants for manuscript drafting assistance, LaTeX typography checking, and automated test harness refactoring via the paperloop framework. Specifically, LLM-powered tools assisted in organizing benchmark summaries, refactoring test harnesses, and formatting the double-blind review artifact. All references were subsequently verified by the authors against primary sources (publisher records or the live specification pages); an incorrect author list introduced in an earlier revision was found and corrected by that process. The core conceptual architecture, security proofs, empirical findings, and intellectual claims remain entirely the original work of the authors.

\section{Ethics and Dual-Use Analysis}\label{sec:appendix_ethics}
The research presented in this paper introduces a defensive mechanism to ensure the security and provenance of autonomous agent releases. This work does not involve human subjects, does not analyze personally identifiable information, and does not conduct active measurements against production systems or networks operated by third parties. The \scoredVectors{} adversarial tamper vectors and the \corpusProfiles{}-profile corpus are fully synthetic and generated within isolated, local test environments for the sole purpose of evaluating the proposed cryptographic and policy enforcement mechanisms. 

\paragraph{Dual-Use Considerations.}
Cryptographic execution attestation mechanisms, if repurposed maliciously, could theoretically be employed to construct invasive employee monitoring frameworks or enforce rigid software lock-in. EviAssure mitigates these dual-use concerns by enforcing decentralized multi-signature threshold governance and open policy schemas (\nolinkurl{governance/release_policy.yaml}), and by keeping raw payloads out of evidence bundles entirely --- traces carry digests only. Salted HMAC blinding (\texttt{blind\_payload}) additionally unlinks the same recorded action across deployments, though as Section~\ref{sec:witness} states, it does not conceal payload equality within one deployment.

\paragraph{Data Privacy \& Parameter Blinding.}
Autonomous software agents frequently process confidential prompt texts, customer financial details, or proprietary source code snippets. To prevent public release audit logs from exposing sensitive data, EviAssure keeps raw payloads out of evidence bundles entirely---traces carry digests only---and the blinding protocol additionally re-keys those digests with a per-deployment salted HMAC ($H_{\text{blinded}}$), so the same recorded action is not linkable across environments. Within one environment the deterministic construction still reveals repetition (Definition 3). This preserves full Merkle tree inclusion auditability without storing proprietary inputs in the audit log.

\paragraph{Responsible Disclosure \& Safety Impact.}
EviAssure raises the cost of forging or replaying release evidence produced by a compromised runner. It does \emph{not} prevent prompt injection: an injected agent produces genuine actions that the collector honestly records and the gate correctly approves, and Section~\ref{sec:twolayer} measures this directly --- all \insRecallN{} anomalous corpus profiles, including the injection class, are APPROVED. The contribution is that such a release becomes \emph{undeniable and attributable} after the fact, not that it is prevented. An earlier version of this appendix claimed prevention; that claim contradicted our own Section~\ref{sec:twolayer} results and has been withdrawn.

\section{Deployment Trade-offs}\label{sec:tradeoffs}
Two engineering consequences follow. \emph{Audit granularity}: since a depth-bound proof costs \sparseProofSizeKb{}\,KB against \rawSizeOneMKb{}\,KB of raw trace, organisations can archive payloads to cold storage and keep only proofs online. \emph{Replay state}: the gate must hold state across submissions, which V16 shows it cannot do across replicas without shared storage; a key-value store with atomic set-if-absent and TTL $\Delta T_{\text{max}}$ is the natural implementation, and it must fail closed when that store is unreachable rather than admit a split-brain replay. Neither deployment is measured here.

\section{Open Science Appendix}\label{sec:appendix_open_science}
The reproducibility materials consist of the complete EviAssure implementation (cryptographic attestation, blinding, held-out inspection, and policy verification modules), the \corpusProfiles{}-profile agent execution trace corpus, the \scoredVectors{}-vector tamper harness with negative controls, the executed DSSE/TUF/OPA baselines, production GitHub Action workflows, benchmark scripts, and the paper figure generator. Furthermore, all datasets used to establish comparative baselines (e.g., the specific OPA Rego policies and JSON payloads used to evaluate the baseline) are included to guarantee full baseline reproducibility.

\subsection{Anonymous Review Artifact \& Verification}
To comply with USENIX Security '27 Open Science guidelines while preserving double-blind review, the complete artifact has been published to a sanitized, anonymous repository at:
\begin{center}
\url{https://anonymous.4open.science/r/eviassure-release-assurance/}
\end{center}

Additionally, an archived review package \texttt{eviassure\_usenix27\_artifact.zip} (size: \artifactSizeKb{} KB) has been prepared using \nolinkurl{scripts/prepare_anonymous_artifact.py}. The archive carries SHA-256 digest:
\begin{center}
\texttt{\artifactShaPartOne}\\
\texttt{\artifactShaPartTwo}
\end{center}

\subsection{Reproduction Workflow}
To execute full reproduction of paper figures, benchmark results, and unit tests:
\begin{enumerate}
    \item \textbf{Environment Setup}: Install dependencies via \texttt{pip install -e .} (Python $\ge 3.10$).
    \item \textbf{Unit \& Integration Test Suite}: Run \texttt{pytest tests/ -v} (\testCount{} tests). The suite includes property-based crypto soundness tests, trusted-key-registry tests, and regression tests for every bypass fixed in the paper's revision history; \texttt{test\_docs\_consistency.py} additionally binds the manuscript's stated test count to the collected count, so the two cannot drift.
    \item \textbf{Attestation Scaling \& Throughput Microbenchmarks}: Run \texttt{python3 scripts/run\_release\_benchmark.py --repeats 5} to generate \nolinkurl{results/benchmark_summary.json}.
    \item \textbf{Adversarial \& Baseline Evaluation}: Run \texttt{python3 scripts/run\_security\_eval.py {-}{-}require-executed} to execute the \scoredVectors{}-vector suite, the clean negative controls, the wire-fuzzing campaign, the executed DSSE/TUF/OPA baselines, the ablation matrix and the held-out inspection protocol into \nolinkurl{results/security_evaluation.json}. These results are platform-independent. \texttt{{-}{-}require-executed} fails rather than silently substituting a modeled baseline.
    \item \textbf{Corpus Layered Evaluation}: Run \texttt{python3 scripts/generate\_trace\_corpus.py} then \texttt{python3 scripts/run\_corpus\_eval.py} to rebuild the \corpusProfiles{}-profile corpus and evaluate it through the integrity, policy and held-out inspection layers.
    \item \textbf{Figure Generation \& Manuscript PDF Rebuilding}: Run \texttt{python3 scripts/generate\_paper\_pdf.py} to regenerate paper plots and recompile \nolinkurl{docs/usenix_paper_manuscript.pdf}.
\end{enumerate}

\end{document}
