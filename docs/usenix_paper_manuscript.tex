\documentclass[letterpaper,10pt,twocolumn]{article}
\usepackage{usenix}
% NOTE: do NOT add \usepackage{lmodern} here. usenix.sty loads mathptmx
% (Times Roman); lmodern loaded afterwards silently overrides it, and the CFP
% requires "10-point Times Roman type" and forbids changing font defaults.
% An earlier revision carried lmodern and shipped a Latin Modern PDF.

\usepackage{amsmath,amssymb,amsfonts,amsthm}
\DeclareMathAlphabet{\mathcal}{OMS}{cmsy}{m}{n}
%\renewcommand{\ttdefault}{cmtt}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{microtype}
\usepackage{url}
\usepackage{hyperref}
\usepackage{xcolor}
\usepackage{subcaption}
\usepackage{listings}
\usepackage{enumitem}
\usepackage{tikz}
\raggedbottom
\usetikzlibrary{shapes.geometric, arrows, positioning, fit, backgrounds, calc}
% NOTE: no \setlist whitespace compression. The USENIX Security '27 CFP:
% "Any attempts to remove whitespace (e.g., negative vspaces, savetrees,
%  titlesec, removing author blocks, etc.) or change the formatting defaults
%  (e.g., fonts) are forbidden and may result in rejection."
% Page limits are met by cutting content, which is what the CFP requires.

\lstdefinelanguage{json}{
  stringstyle=\color{red},
  morestring=[b]",
  morestring=[d]'
}

\lstset{
  basicstyle=\ttfamily\scriptsize,
  breaklines=true,
  frame=single,
  captionpos=b,
  keywordstyle=\color{blue},
  stringstyle=\color{red},
  commentstyle=\color{gray}
}

\hypersetup{
    colorlinks=true,
    linkcolor=black,
    citecolor=blue!70!black,
    urlcolor=blue!80!black
}

\theoremstyle{definition}
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{definition}{Definition}

% Extra inter-word stretch before TeX resorts to an overfull line.
% Changes no page geometry.
\setlength{\emergencystretch}{3em}
\sloppy

% 2026-08-16: corpus_evaluation.json regenerated (note wording); Layer 1/2/3
% numbers re-confirmed against the artifact (test_corpus_eval_json.py binds them).
\input{frozen_metrics}
\input{security_metrics}
\input{artifact_digest}

\begin{document}

\title{\Large \bf Evidence-Backed Release Assurance for Autonomous Agent Deployments:\\
Cryptographic Attestation, Fail-Closed Gates, and Tamper-Resilient Audit Infrastructure}

% ---------------------------------------------------------------------------
% AUTHOR BLOCK -- ANONYMITY SWITCH
%
% USENIX Security '27 is DOUBLE-BLIND. The CFP is explicit:
%   "Papers must be submitted for anonymous review: no author names or
%    affiliations may appear on the title page and authors must not reveal
%    their identity in the text."
%   "Papers that are not properly anonymized may be rejected without review."
%
% So the submission build MUST stay anonymous. The real author block lives in
% the \else branch and is used for the camera-ready, arXiv, and internal
% copies. Flip ONE line to switch; never edit the block itself for a build.
%
%   \anonymoustrue   -> submission        (DEFAULT -- upload this to HotCRP)
%   \anonymousfalse  -> camera-ready      (after acceptance)
%
% Full contact details, roles and affiliations are recorded in AUTHORS.md,
% which is deliberately excluded from the anonymous review artifact.
% ---------------------------------------------------------------------------
\newif\ifanonymous
\anonymoustrue          % <-- SUBMISSION BUILD. Set to \anonymousfalse for camera-ready.

\ifanonymous
  \author{{\rm Anonymous Submission}}
\else
  \author{
  {\rm Petar Radanliev}\\
  The Open University\\
  The Alan Turing Institute\\
  \normalsize\nolinkurl{radanliev@gmail.com}
  }
\fi

\maketitle

\begin{abstract}
Release pipelines for autonomous agents increasingly gate promotion on attested execution traces: signed records of what the agent did during its evaluation run. These attestations share an unaddressed weakness. The evidence collector is the only observer of the run, so an adversary who controls the agent and the collector need not forge or alter anything: it performs an action and does not record it. The resulting bundle is internally consistent, every signature verifies, and every integrity check passes, because nothing was tampered with.

We define \emph{Witnessed Trace Completeness}, a completeness property stated relative to a declared set of \emph{witnesses} --- the tool endpoints, API gateways and sandbox brokers that actually serve agent actions. Each witness serves only sessions opened by an orchestrator-signed credential, issues receipts carrying a monotonic per-session sequence number and, at session end, signs a commitment to how many actions it served. A stateful, fail-closed release gate then \emph{reconciles} the submitted trace set against those receipts and closings for the session it credentialed, rather than verifying a document. We prove that, under EUF-CMA security of the witness and orchestrator signatures, an adversary holding the agent, the collector and the collector's key cannot omit, fabricate, relabel or substitute a witnessed action; and we show that no verifier whose verdict is a function of one submitted document can achieve witnessed completeness or replay-freedom, which places record-format standards on one side of a structural boundary and stateful reconciliation on the other.

We implement the protocol in \textsc{EviAssure}, an evidence-bundle format that rides inside in-toto/SLSA envelopes with a domain-separated, depth-bound Merkle trace commitment, and evaluate it against executed in-toto/DSSE and TUF verifiers and re-implemented receipt and hash-chain designs. Against \omVectors{} omission attacks and an honest control, reconciliation detects \omWtc/\omVectors{}, receipt-only verification \omReceipts/\omVectors{}, and per-issuer chaining \omChain/\omVectors{}; on the orthogonal tampering axis the gate blocks \eviBlocked/\eviTotal{} vectors (95\% CI [\eviCIlo, \eviCIhi]) against \compBlocked{} for a composed in-toto/DSSE + TUF + OPA pipeline, and we report the one it does not. We state the guarantee's boundary as plainly as the guarantee: it is exactly as wide as the deployed witness set, it says nothing about actions no witness mediates, and it approves a semantically misbehaving trace that is honestly recorded --- held-out inspection over a \corpusProfiles{}-profile corpus recovers \insOvertK/\insOvertN{} structurally overt anomalies and \insStealthK/\insStealthN{} disguised ones.
\end{abstract}

\section{Introduction}
An autonomous agent is promoted to production --- a new checkpoint, a widened tool scope, a new pipeline stage --- on the strength of an evaluation run: a test suite, a red-team script, a canary task. Unlike a compiled artifact, whose behaviour is fixed by its code, the agent's behaviour during that run is a non-deterministic sequence of tool calls~\cite{jimenez2024swebench, yao2023react, greshake2023not}, and the security question at the gate is not \emph{what the agent is} but \emph{what it did}. Supply-chain provenance answers the first question well: in-toto, SLSA and Sigstore attest the origin and integrity of static artifacts~\cite{torres2019in-toto, slsa2023supply, sigstore2022cosign}, and that such attestation is needed is well established --- taxonomies of documented supply-chain attacks enumerate hundreds of real incidents~\cite{ladisa2023sok, okafor2022sok}, package-manager compromise is commonplace in interpreted-language ecosystems~\cite{duan2021towards}, and update systems have required dedicated defences since 2010~\cite{samuel2010surviving, cappos2008look, moore2023artemis}. In-toto's runtime-trace and agent-decision predicates and the 2026 agent-provenance drafts extend the same record formats to execution traces~\cite{intoto_runtime_trace, intoto2026agent-decision, apas2026agent-provenance, aas12026agent-audit}, and receiver-attested receipt schemes give each recorded action an attestation from a party other than the agent~\cite{notarized2026agents, pipelab2026receipts}.

All of these verify a \emph{document}. The document is produced by the evidence collector, and the collector is the sole observer of the run. Consider an execution $A \rightarrow B \rightarrow C \rightarrow D \rightarrow E$ in which $C$ is the action a reviewer would have blocked. An adversary who controls the agent and the collector --- a compromised runner, a poisoned build step, an agent that has been prompt-injected into rewriting its own log~\cite{greshake2023not} --- does not forge $C$'s absence. It performs $C$, and records $A \rightarrow B \rightarrow D \rightarrow E$. Every signature over that document verifies; the Merkle root, hash chain or receipt set is consistent, because each was computed over what was recorded. A receipt proves an action happened; a set of receipts does not prove that no other action happened. We call this the \emph{omission} attack, to distinguish it from tampering, and we show (Section~\ref{sec:omission}) that executed in-toto/DSSE and TUF verifiers, per-issuer hash chains and per-action receipts each fail it, for a reason that Theorem~\ref{thm:separation} makes structural rather than incidental.

Our approach moves the observation out of the collector's trust domain. The components that already serve an agent's externally observable actions --- the database proxy, the API gateway, the sandbox broker --- become \emph{witnesses}: each holds a signing key the adversary does not control, serves only sessions opened by an orchestrator-signed credential, keeps a monotonic counter per session, returns a signed receipt $(\mathit{sid}, \mathrm{seq}, H_{\mathrm{act}})$ with every action it serves, and at session end signs a \emph{closing} $(\mathit{sid}, n_j)$ committing to how many actions it served. The release gate holds state and \emph{reconciles}: for the session it credentialed, every receipt authenticates under a registry-pinned witness key, every witness's receipts form exactly $1,\dots,n_j$, every receipt binds to a submitted trace, and every trace that claims a mediated action carries a receipt. Omitting, fabricating, relabelling or substituting a witnessed action now requires forging a witness or orchestrator signature (Theorem~\ref{thm:wtc}). The guarantee is stated \emph{relative to} the witness set: an action no witness mediates is outside it, and we say so, because a guarantee whose boundary is declared can be audited and one that assumes a perfect collector cannot.

We implement the protocol in \textsc{EviAssure}\footnote{The complete anonymous source code, evaluation corpus, and benchmark suite are available at \url{https://anonymous.4open.science/r/eviassure-artifact-781D/}.}: an evidence bundle that is an in-toto Statement carrying a domain-separated (RFC~6962), depth-bound Merkle commitment to the trace set, a signed trace count, the witness receipts and closings, and a nonce; and a fail-closed gate whose verdict is a deterministic function of the bundle, a pinned policy, a pinned key and witness registry, the credentialed session, and replay state. The bundle rides inside existing SLSA/in-toto tooling unmodified; what the gate adds is the state and the second trust domain a document cannot carry.

\subsection{Contributions}
\begin{enumerate}
    \item \textbf{Witnessed Trace Completeness} (Definition~\ref{def:wtc}): a completeness property relative to a declared witness set, a protocol that achieves it with credentialed sessions, sequence-bound receipts and signed closing counts, and a proof (Theorem~\ref{thm:wtc}) that omission, fabrication, relabelling or substitution of a witnessed action requires a witness-key or orchestrator-key forgery.
    \item \textbf{A separation between document verification and stateful reconciliation} (Theorem~\ref{thm:separation}): a verifier whose verdict is a function of a single submitted document achieves neither witnessed completeness nor replay-freedom. We use it to place in-toto/SLSA/TUF, the agent-provenance drafts and receipt schemes on one side of the boundary and the gate on the other, and to explain the omission results.
    \item \textbf{An evidence-bound, fail-closed release gate}: a bundle format compatible with in-toto v0.2 / SLSA v1.0 envelopes, a domain-separated, depth-bound Merkle trace attestation with truncation-resistant count binding, and a gate whose verdict is reproducible under a pinned policy version and holds replay state.
    \item \textbf{A falsifiable evaluation with executed baselines and disclosed failures}: \omVectors{} omission attacks with an honest-execution control against executed in-toto/DSSE and TUF verifiers and re-implemented receipt and hash-chain designs; \scoredVectors{} tamper vectors, five of them authored against the design rather than the check list, with clean negative controls; a held-out inspection protocol; Wilson intervals on every rate; and an explicit account of the vector the gate does not block, the anomaly class inspection does not see, and the actions no witness covers.
\end{enumerate}

\paragraph{Record formats versus authorization layers.}
in-toto and SLSA define \emph{passive record formats}: they attest what occurred, and supply no stateful layer that gates a release on a non-deterministic trace. EviAssure is that layer --- an active, fail-closed gate that holds replay state, binds the trace by a signed count and witness reconciliation, and decides under a pinned policy version --- and Theorem~\ref{thm:separation} makes the distinction formal. Composition is preserved: an \texttt{EvidenceBundle} is an in-toto v0.2 / SLSA v1.0 envelope, so existing records ride inside it unmodified. Section~\ref{sec:related-2026} compares against the 2026 agent-provenance standards and receipt schemes.

\paragraph{Open Science and Reproducibility.}
The full implementation, the \corpusProfiles{}-profile execution trace corpus, the \scoredVectors{}-vector attack harness with its clean negative controls, the omission suite, and the executed baselines (in-toto/DSSE, TUF, and the Open Policy Agent (OPA) Rego policy) are released as a sanitized anonymous review artifact (Appendix~\ref{sec:appendix_open_science}).

\section{Preliminaries}\label{sec:preliminaries}
\subsection{Formal Security Definitions}

\begin{definition}[Unforgeability under Chosen Message Attack (EUF-CMA)]
An Evidence Assurance Scheme $\Sigma = (\text{KeyGen}, \text{Package}, \text{Verify})$ is EUF-CMA secure if for all probabilistic polynomial-time adversaries $\mathcal{A}$, the probability that $\mathcal{A}$ outputs a valid pair $(E^*, \sigma^*)$ such that $\text{Verify}(PK, E^*, \sigma^*) = 1$ without $E^*$ having been signed by an honest party holding $SK$ is negligible in security parameter $\lambda$:
\begin{equation}
\text{Pr}[\text{Exp}_{\Sigma, \mathcal{A}}^{\text{euf-cma}}(\lambda) = 1] \le \text{negl}(\lambda)
\end{equation}
\end{definition}

\begin{definition}[Merkle Trace Inclusion Soundness]
For root $R_M$ committed together with its leaf count $N$, and leaf $H_i$, a Merkle proof $\pi_i$ is sound if no computationally bounded adversary can construct $\pi'_i$ for any $H'_i \neq H_i$ --- including internal node digests of the same tree --- such that $\text{VerifyMerkle}(H'_i, \pi'_i, R_M, d) = 1$ for the committed depth $d = \lceil\log_2 N\rceil$, where verification accepts only proof paths of exactly length $d$ and leaves and internal nodes are hashed under disjoint domain prefixes.
\end{definition}

\begin{definition}[Evidence Integrity]\label{def:integrity}
An evidence pack $(R_M, \sigma, N)$ comprising a Merkle root $R_M$, signature $\sigma$, and signed trace count $N$ provides \emph{evidence integrity} if no probabilistic polynomial-time adversary, given oracle access to the honest packager, can produce a verifying pack $(R'_M, \sigma', N')$ that the packager never issued; equivalently, nothing that was \emph{recorded} can be modified, reordered, inserted, or removed without detection. Integrity is a property of the signed record and is guaranteed by Theorem~\ref{thm:failclosed} below.
\end{definition}

\begin{definition}[Trace Completeness]\label{def:completeness}
Let $E$ be an agent execution and let $\mathcal{C}$ be the evidence collector that observes $E$. A recorded trace set $T = \{T_1, \dots, T_N\}$ is \emph{complete with respect to $E$} if every action of $E$ that $\mathcal{C}$ observed is represented by exactly one $T_i$. Completeness is an observation property of the \emph{collector}, not of the Merkle tree: the tree attests exactly the leaves it was given. If a real execution is $A \rightarrow B \rightarrow C \rightarrow D \rightarrow E$ but the recorded trace is $A \rightarrow B \rightarrow D \rightarrow E$, the tree protects the recorded sequence perfectly and says nothing about $C$. No signature, hash chain, or inclusion proof over the submitted set can, because the adversary forged nothing --- it declined to create evidence. Definition~\ref{def:wtc} makes this attainable by moving the observation out of the collector's trust domain.
\end{definition}

\begin{definition}[Witnessed Trace Completeness]\label{def:wtc}
Let $\mathcal{W}=\{W_1,\dots,W_m\}$ be a set of \emph{witnesses}, each holding a signing key the adversary does not control, and let $\mathsf{med}(W_j)$ be the set of action types $W_j$ mediates. An action is \emph{witnessed} if its type lies in $\bigcup_j \mathsf{med}(W_j)$. Let $\mathcal{O}$ be the \emph{orchestrator}, the trusted controller that opens a release evaluation: for release $\mathit{rid}$ it issues one \emph{session credential} $\mathrm{Sign}_{\mathcal{O}}(\mathit{sid}, \mathit{rid})$, and every witness serves a request only if it carries a credential that verifies under $\mathcal{O}$'s pinned key. A submitted trace set $T$ for release $\mathit{rid}$ is \emph{witness-complete} if $T$ declares the session $\mathit{sid}$ that $\mathcal{O}$ credentialed for $\mathit{rid}$, and for every $W_j \in \mathcal{W}$ there is a $W_j$-signed closing $(\mathit{sid}, n_j)$, the $W_j$-signed receipts in $T$ are exactly the contiguous sequence $1,\dots,n_j$ for $\mathit{sid}$, each receipt binds to a distinct member of $T$, and every witnessed action in $T$ carries a receipt.

Completeness is stated \emph{relative to} $\mathcal{W}$: an action of a type no witness mediates is outside the guarantee. This is a deployment-controllable condition --- mediate every externally observable effect --- rather than the unbounded assumption that the collector observed everything.
\end{definition}

\begin{theorem}[Witnessed Completeness Soundness]\label{thm:wtc}
Let $\mathcal{A}$ control the agent, the evidence collector and the collector's signing key, but no key in $\mathcal{W}$ and not $\mathcal{O}$'s key. If the gate accepts a witness-complete bundle for release $\mathit{rid}$ with credentialed session $\mathit{sid}$, then except with probability $(m+1) \cdot \mathrm{Adv}^{euf\text{-}cma}_{\mathrm{Ed25519}}$, the trace set contains every witnessed action performed for $\mathit{rid}$, and every witnessed action it contains was performed.
\end{theorem}

\begin{proof}
A witnessed action performed for $\mathit{rid}$ is served by some $W_j$ under a credential; since $\mathcal{O}$ issues one credential per release, that credential names $\mathit{sid}$ unless $\mathcal{A}$ minted one, an Ed25519 forgery under $\mathcal{O}$'s key. So suppose $\mathcal{A}$ omits a witnessed action $a$ served by $W_j$ under $\mathit{sid}$ as its $k$-th action, and the gate accepts. $W_j$'s counter is monotone and advances only on service, so $W_j$ issued receipts $1,\dots,n_j$ with $k \le n_j$, and its closing commits to $n_j$. Acceptance requires the receipt sequence for $W_j$ to be exactly $1,\dots,n'_j$ with a matching closing. Three cases. (i) $n'_j = n_j$: then $\mathcal{A}$ supplied $n_j$ distinct receipts, but only $n_j - 1$ correspond to submitted actions, so some receipt is unmatched and reconciliation rejects --- unless $\mathcal{A}$ minted a receipt, an Ed25519 forgery under $W_j$. (ii) $n'_j < n_j$: the presented closing differs from the one $W_j$ signed, so $\mathcal{A}$ forged a closing under $W_j$. (iii) $\mathcal{A}$ substitutes a receipt from another session $\mathit{sid}' \neq \mathit{sid}$, or presents a complete bundle for $\mathit{sid}'$: the session identifier is inside the signed receipt and closing payloads and the gate reconciles only against the $\mathit{sid}$ it credentialed for $\mathit{rid}$, so both are rejected without forgery. Each surviving case is a forgery against one of $m$ witness keys or the orchestrator key; a union bound gives the stated advantage. Fabrication of a witnessed action is symmetric: the action carries no receipt, and minting one is again a forgery. $\square$
\end{proof}

\begin{theorem}[Record Formats Cannot Achieve These Properties]\label{thm:separation}
Let $V$ be a \emph{stateless document verifier}: a deterministic predicate $V(d) \in \{\mathrm{accept},\mathrm{reject}\}$ over a single submitted document $d$, with access to fixed public parameters (trust roots, policy) but no state that varies with prior submissions and no channel to $\mathcal{W}$. Then $V$ achieves neither replay-freedom nor witnessed completeness.
\end{theorem}

\begin{proof}
\emph{Replay-freedom.} Determinism gives $V(d)=V(d)$: if $V$ accepts $d$ once it accepts every resubmission, so an adversary replays an approved release indefinitely. Distinguishing first from subsequent submission requires state that varies across submissions, which $V$ by definition lacks.

\emph{Witnessed completeness.} Fix a witness $W$ and consider two executions: $E_1$ serving $n$ actions, and $E_2$ serving the same first $n-1$ and then a further action whose record the adversary omits. Let $d_1, d_2$ be the honestly-signed documents. Every attestation in $d_2$ is genuine: the collector signs truthfully over what it recorded, and each retained receipt is authentic. So $d_1$ and $d_2$ are identically distributed over every field $V$ can inspect, and $V(d_1)=V(d_2)$. Since $V$ must accept $d_1$ (a correct execution), it accepts $d_2$. The only field that could differ is a commitment to $W$'s served count, which $W$ alone can produce --- and requiring it makes the verifier's input depend on a trust domain outside $d$, contradicting statelessness.
\end{proof}

Theorem~\ref{thm:separation} is why we describe in-toto, SLSA, TUF, AAS-1, \texttt{agent-decision} and receipt schemes as record formats rather than as weaker gates. Their verification is by construction a function of the submitted document; the gap is structural, not a matter of maturity, and Section~\ref{sec:omission} measures it.

\section{System Architecture and Threat Model}\label{sec:system_threat}

\textbf{Intuition.} EviAssure turns an agent's step-by-step execution path into a signed, Merkle-committed evidence bundle before deployment, has the components that served the agent's actions attest to how many they served, and evaluates the bundle at a \emph{fail-closed gate} that denies release whenever a required proof, signature, receipt, closing or threshold is missing, malformed, or belongs to a different session.

\subsection{System Model}
The architecture involves six entities (Figure~\ref{fig:architecture}): the \emph{Agent}, the \emph{Evidence Collector}, the \emph{Witnesses}, the \emph{Orchestrator}, the \emph{Registry}, and the \emph{Release Assurance Gate}.

\textbf{Agent and Evidence Collector}: the agent runtime and the component that records its actions into an \texttt{EvidenceBundle} signed under the collector key. Both are in the adversary's tier. In production the collector is an out-of-process monitor outside the agent's sandbox, observing activity via eBPF syscall tracepoints and container network proxies; \emph{in this evaluation it is Python instrumentation wrappers} (Section~\ref{sec:limitations}). Signing authority for the collector key is rooted outside the runner in a KMS/HSM; the gate checks the declared key ARN against \texttt{kms\_key\_arn\_pattern}, a misconfiguration control rather than an adversarial one.
\textbf{Witnesses}: the tool endpoints, API gateways and sandbox brokers that actually serve the agent's actions. Each holds its own key, verifies the session credential before serving, issues a receipt per action and a closing per session (Section~\ref{sec:witness}). They are a separate trust domain from the collector.
\textbf{Orchestrator}: the trusted CI controller that opens a release evaluation by issuing one signed session credential per release and, at the end, tells the gate which session it credentialed. In the reference implementation the same process later runs the gate.
\textbf{Registry}: an out-of-band store for release policies, the pinned collector keys, the pinned witness keys with the action types each mediates, the pinned orchestrator key, and the revocation list.
\textbf{Release Assurance Gate}: the enforcement entity, running in a trusted CI/CD step, evaluating the signed \texttt{EvidenceBundle} against the pinned policy and reconciling it against the witnesses for the credentialed session.

\begin{figure*}[t]
\centering
\begin{tikzpicture}[x=1mm, y=1mm,
    box/.style={rectangle, draw=#1!80!black, fill=#1!12, rounded corners=3pt, minimum width=19mm, minimum height=8mm, align=center, font=\small, thick, inner sep=2pt},
    gate_box/.style={rectangle, draw=blue!85!black, fill=blue!15, rounded corners=3pt, minimum width=26mm, minimum height=10mm, align=center, font=\small\bfseries, very thick, inner sep=2pt},
    zone/.style={draw=#1, dashed, rounded corners=5pt, fill=#1!3},
    flow/.style={thick, ->, >=stealth, draw=black!75},
    secflow/.style={very thick, ->, >=stealth, draw=blue!80!black},
    wflow/.style={very thick, ->, >=stealth, draw=orange!80!black},
    lbl/.style={font=\small, fill=white, inner sep=1.5pt, rounded corners=1pt, draw=gray!30}
]

\begin{scope}[on background layer]
\draw[zone=red] (-90, -22) rectangle (-44, 22);
\node[anchor=north, font=\small\bfseries\color{red!80!black}] at (-67, 22) {Adversary tier: agent, collector, $K_C$};
\draw[zone=orange!80!black] (-39, -22) rectangle (-7, 22);
\node[anchor=north, font=\small\bfseries\color{orange!80!black}] at (-23, 22) {Witnesses (own keys)};
\draw[zone=green!60!black] (-2, -22) rectangle (56, 22);
\node[anchor=north, font=\small\bfseries\color{green!60!black}] at (27, 22) {Trusted control plane};
\draw[zone=green!70!black] (61, -22) rectangle (85, 22);
\node[anchor=north, font=\small\bfseries\color{green!70!black}] at (73, 22) {Target};
\end{scope}

\node[box=orange] (agent) at (-78, 9) {\textbf{Agent}\\untrusted runtime};
\node[box=red] (collector) at (-59, -11) {\textbf{Collector}\\signs bundle $\mathcal{E}$ with $K_C$};

\node[box=orange] (w1) at (-23, 13) {\textbf{DB proxy $W_1$}\\counter per $\mathit{sid}$};
\node[box=orange] (w2) at (-23, 0) {\textbf{API gateway $W_2$}\\counter per $\mathit{sid}$};
\node[box=orange] (w3) at (-23, -13) {\textbf{Sandbox broker $W_3$}\\counter per $\mathit{sid}$};

\node[box=gray] (orch) at (11, 12) {\textbf{Orchestrator}\\issues $\mathrm{Sign}_{\mathcal{O}}(\mathit{sid},\mathit{rid})$};
\node[box=gray] (registry) at (42, 12) {\textbf{Registry}\\$K_C$, $W_j$, $\mathcal{O}$, $\mathcal{P}$, CRL};
\node[gate_box] (gate) at (26, -10) {Release Gate\\\mdseries verify $\mathcal{E}$; hold nonce state;\\\mdseries reconcile receipts \& closings};

\node[box=green!70!black] (prod) at (73, -10) {\textbf{Production}\\cluster};

\draw[flow] (agent.east) -- node[above=1pt, sloped, lbl, pos=0.55] {action $a$ + credential} (w1.west);
\draw[flow] (agent.east) -- (w2.west);
\draw[flow] (agent.east) -- (w3.west);
\draw[wflow] (w3.west) -- (collector.east);
\node[lbl, text=orange!60!black] at (-42, -19) {receipts $r$, closings $c_j$};
\draw[flow] (orch.north) -- (11, 28) -- (-78, 28) -- (agent.north);
\node[lbl] at (-34, 28) {session credential $\mathrm{Sign}_{\mathcal{O}}(\mathit{sid},\mathit{rid})$};
\draw[secflow] (collector.south) -- (-59, -28) -- (26, -28) -- (gate.south);
\node[lbl] at (-17, -28) {bundle $\mathcal{E}$ = traces, root, $N$, receipts, closings, nonce, $\mathit{sid}$; signed with $K_C$};
\draw[flow] (orch.south) -- node[left=2pt, lbl] {expected $\mathit{sid}$} (16, -5);
\draw[flow] (registry.south) -- node[right=2pt, lbl] {$\mathcal{P}$, keys} (37, -5);
\draw[flow, draw=green!60!black, very thick] (gate.east) -- node[above=2pt, lbl] {\textbf{APPROVED}} (prod.west);
\end{tikzpicture}
\caption{EviAssure with witnesses. The adversary controls the agent, the collector and the collector's key $K_C$, but no witness key and not the orchestrator's key. Witnesses serve only requests carrying the orchestrator's session credential, sign a receipt for every action they serve and a closing count at session end; the collector packages traces and attestations into a bundle signed under $K_C$; the gate verifies the bundle, holds replay state, and reconciles the trace set against receipts and closings \emph{for the session the orchestrator credentialed}, so an omitted, fabricated, relabelled or substituted witnessed action requires a witness-key or orchestrator-key forgery. Actions outside every witness's mediated set are outside the guarantee.}
\label{fig:architecture}
\end{figure*}

\subsection{Adversary Model}\label{sec:threat}
The security model considers a powerful adversary $\mathcal{A}$ attempting to deploy an unapproved, regression-prone, or malicious agent update to production environments~\cite{shostack2014threat, carlini2024poisoning, schuster2021get}. $\mathcal{A}$ may control the agent, the evidence collector and the collector's signing key, intermediate runner environments, network transit layers, and build scripts.

\paragraph{Trust Model Boundaries.} EviAssure assumes Ed25519 and SHA-256 remain computationally intractable for PPT adversaries, and that the Cloud KMS / HSM faithfully enforces identity access control over private signing keys. Witness keys and the orchestrator's credential key are distinct trust domains: the adversary holds neither, and Section~\ref{sec:limitations} states what degrades when that assumption is weakened. It further requires strict environment isolation: an agent that escapes its sandboxed tier to obtain code execution on the gate, a witness or the orchestrator can forge attestations \emph{prior} to signing. Compromise of the gate's trusted computing base, of a witness, or of the orchestrator is therefore an explicit out-of-scope limitation --- the cryptographic guarantees hold assuming a tamper-proof control plane. HSM side channels, subversion of the provider's root KMS keys, and hypervisor rootkits are likewise out of scope.

We categorize $\mathcal{A}$'s in-scope capabilities into six classes. Five are mapped to the tamper vectors of Table~\ref{tab:tamper}: runner and memory tampering ($\mathcal{A}_1$), network and signature forgery ($\mathcal{A}_2$), replay and time skew ($\mathcal{A}_3$), governance bypass ($\mathcal{A}_4$), and serialization malleability ($\mathcal{A}_5$). The sixth, \emph{collector omission} ($\mathcal{A}_6$), is the adversary of Theorem~\ref{thm:wtc}: it performs actions and declines to record them, relabels them into another session, or presents another session's bundle, and it is mapped to the omission vectors of Table~\ref{tab:omission}.
\subsection{Security Requirements}
The construction is proved against three requirements, subject to the trust boundaries above; the evaluation in Section~\ref{sec:eval} is organised to test each in turn.

\begin{enumerate}
    \item \textbf{Evidence Integrity}: no recorded action can be modified, reordered, inserted, or removed without detection (Definition~\ref{def:integrity}).
    \item \textbf{Trace Completeness}: the attestation must bind the count of recorded actions so that truncation is not silent (Definition~\ref{def:completeness}) --- and, because a count the collector itself signs cannot constrain the collector, must be strengthened to hold \emph{relative to a witness set} (Definition~\ref{def:wtc}).
    \item \textbf{Release Authorization Soundness}: the decision must be a deterministic function of signed evidence, the credentialed session and pinned policy. No PPT adversary should induce an APPROVED verdict for modified, incomplete or substituted traces.
\end{enumerate}

The gate enforces these fail-closed: any payload failing verification is rejected with exit code 1 (\texttt{BLOCKED}). Requirement 2 is where every prior system stops at the \emph{collection boundary}~\cite{apas2026agent-provenance, aas12026agent-audit}; Section~\ref{sec:witness} is our attempt to move it.

\section{Cryptographic Evidence Attestation}
EviAssure establishes verifiable release claims by packaging agent action histories into cryptographically attested evidence bundles. This section describes the Merkle tree aggregation protocol, browser Document Object Model (DOM) attestation, trace parameter blinding, and SLSA v1.0 / in-toto predicate envelopes.

\subsection{Execution Trace \& Browser UI Attestation Schema}
Each execution trace record $T_i$ captures structured action metadata across autonomous agent steps. For API and backend tool calls, $T_i$ records:
\begin{equation}
T_i = (\text{id}_i, \text{agent}_i, \text{action}_i, \text{status}_i, \text{duration}_i, H_{\text{out},i})
\end{equation}
For browser and UI agent steps, EviAssure extends attestation to capture DOM state hashes ($H_{\text{DOM}}$) and visual screenshot render digests ($H_{\text{screen}}$):
\begin{equation}
\begin{split}
T_{\text{UI},i} = (\,&\text{id}_i, \text{agent}_i, \text{action}_i, \text{url}_i,\\
&\text{elem}_i, H_{\text{DOM},i}, H_{\text{screen},i})
\end{split}
\end{equation}
$H_{\text{DOM},i}$ is computed over a deterministically serialized DOM, so identical page states yield identical digests and any mutation --- an injected modal, an altered form control --- changes it; $H_{\text{screen},i}$ digests canvas pixels under deterministic software rasterization (headless browser, GPU and font hinting disabled, pinned viewport), which removes the subpixel drift that would otherwise make an honest re-render fail its own digest. Each record yields a canonical leaf digest $H_i = \text{SHA256}(T_i.\texttt{to\_hash}())$ committing \emph{every} schema field, so editing any recorded field after signing, including \texttt{duration\_ms}, invalidates the recomputed root and is rejected by the gate.

\subsection{Witness Protocol}\label{sec:witness}
A witness is the component that \emph{actually serves} an agent action: a tool endpoint, an API gateway, a database proxy, a sandbox syscall broker. It is not part of the agent runtime, and the security argument rests on two properties: the adversary who owns the agent and the collector holds neither a witness key nor the orchestrator's credential key.

\paragraph{Sessions are opened by the orchestrator, not named by the agent.} A release evaluation begins when the orchestrator issues a session credential $\mathrm{Sign}_{\mathcal{O}}(\mathit{sid}, \mathit{rid})$ for release $\mathit{rid}$ and injects it into the agent's environment. Every witness pins $\mathcal{O}$'s public key and serves a request only if it carries a credential that verifies; a bare session string, or a credential signed by anyone else, is refused and the action is not performed. Without this rule the guarantee below would be empty: an adversary who owns the agent and the collector could label the actions it wants to hide with a different session identifier at the witness and submit a complete, honestly reconciling bundle for the benign one --- omission without a forgery (vector O7, Section~\ref{sec:omission}). With it, relabelling requires forging $\mathcal{O}$'s signature, and the gate binds each release to the one session $\mathcal{O}$ credentialed for it.

Each witness keeps a monotonic counter per session. On serving action $a$ in session $\mathit{sid}$ it returns
\begin{equation}
r = \mathrm{Sign}_{W_j}(\mathit{sid},\ \mathrm{seq},\ H_{\mathrm{act}}(a),\ \mathrm{prev})
\end{equation}
where $H_{\mathrm{act}}$ digests the action identity the witness can bind a receipt to --- in the implementation the trace identifier, agent identifier, action name and output digest; the collector-side \texttt{status} and \texttt{duration\_ms} fields are deliberately not attested by the witness --- and $\mathrm{prev}$ is the digest of the witness's previous receipt in the session, so the receipts also form a per-issuer hash chain and the chaining baseline of Section~\ref{sec:omission} can be evaluated on the same records. At session end, on the orchestrator's broadcast of the credential, $W_j$ emits $\mathrm{Sign}_{W_j}(\mathit{sid}, n_j)$, committing to how many actions it served. \textbf{Every registered witness closes every session}, including with $n_j=0$: without that rule, dropping a witness's actions \emph{and} its closing is indistinguishable from never using the tool, which is vector O3.

The gate reconciles, for the session the orchestrator credentialed: require the bundle to declare that session; authenticate every attestation under a registry-pinned witness key; require the session identifier to match; require each witness's receipts to form exactly $1..n_j$; require every receipt to bind to a submitted trace; and require every trace claiming a mediated action to carry a receipt. The bundle signature covers a digest of the whole attestation set, so a collector cannot delete receipts from a signed bundle and leave the signature intact.

\paragraph{Cost and deployment.} A receipt is one Ed25519 signature over about 200 bytes at the point of service, and the gate performs $O(|T|)$ verifications. The real cost is organisational: witnesses must be deployed at the boundaries that matter, and Definition~\ref{def:wtc} makes the guarantee exactly as strong as that deployment.

Whether such a set can be assembled is the natural objection. A witness is a component that already exists --- an API gateway, a database proxy, a sandbox broker --- and the change is to check a credential and sign what it already logs; witnesses need no agreement with each other, so coverage grows incrementally and a partial deployment is sound over what it does mediate; and the guarantee degrades \emph{visibly}, because an unmediated action type is outside it by definition (Section~\ref{sec:live} reports that residue for a concrete tool set). A perfect-collector assumption cannot be audited at all; a guarantee relative to a declared witness set can.

\paragraph{Parameter blinding.} Bundles carry digests, never raw payloads. A keyed HMAC mode (\texttt{blind\_payload}) additionally re-keys those digests per deployment, which buys unlinkability across environments and resistance to dictionary recovery of low-entropy outputs --- $\mathrm{SHA256}(\texttt{"TOKEN\_VALIDATED"})$ is recoverable by exhaustive search, the salted HMAC is not. It is deterministic, so it does not conceal repetition within one deployment. We record it as a hardening measure rather than a contribution.

\subsection{Merkle Trace Aggregation and Sparse Proof Compression}
To attest $N$ execution traces without transmitting full raw logs to every gate request, EviAssure aggregates normalized trace digests into a SHA-256 Merkle tree~\cite{merkle1989digital, laurie2014certificate}. Each trace record $T_i$ maps to leaf digest $H_i$.

Raw trace logs are \rawSizeOneKKb{}\,KB at $N=1,000$ and \rawSizeOneMKb{}\,KB at $N=10^6$; EviAssure transmits the 32-byte root $R_M$ and $O(\log N)$ inclusion proof paths $\pi_i$ for audited steps. 

Given proof path $\pi_i = \{(P_1, \text{pos}_1), \dots, (P_K, \text{pos}_K)\}$, inclusion is verified recursively by setting $C_0 = H_i$ (the committed leaf digest; leaf digests are already SHA-256 strings and are \emph{not} re-hashed at the proof boundary, matching \texttt{verify\_merkle\_proof}) and evaluating:
\begin{equation}
C_j = \begin{cases}
\text{SHA256}(C_{j-1} \parallel \text{``:''} \parallel P_j), & \text{if } \text{pos}_j = \text{right} \\
\text{SHA256}(P_j \parallel \text{``:''} \parallel C_{j-1}), & \text{if } \text{pos}_j = \text{left}
\end{cases}
\end{equation}
Leaves and internal nodes are hashed under \emph{disjoint domain prefixes} following RFC 6962~\cite{laurie2014certificate}: a leaf commits $\text{SHA256}(\texttt{0x00} \parallel T_i.\texttt{to\_hash}())$ and an internal node commits $\text{SHA256}(\texttt{0x01} \parallel L \parallel R)$ over the raw 32-byte child digests. Hashing both as unprefixed SHA-256 over delimited ASCII, and arguing that a fixed node-preimage length supplies domain separation, does not work: the two input languages merely fail to overlap for one leaf schema, which is an accident of the schema rather than a property of the construction. Explicit prefixes make the separation structural.

The proof is valid if and only if $C_K = R_M$ \emph{and} the path length equals the committed tree depth $d=\lceil\log_2 N\rceil$ derived from the signed execution trace count. The two defences are independent and both are necessary: domain separation prevents an internal digest from ever being interpreted as a leaf, and the depth bound prevents the shortened-path substitution of the CVE-2012-2459 family, which succeeds without any hash collision. Vector V15 (Section~\ref{sec:tamper}) exercises exactly this attack against the third-party auditor path. Transmission size for an audited step remains $<5\text{ KB}$.

\begin{lstlisting}[language=python,caption={Merkle Trace Aggregation Protocol.},label={lst:proto1}]
LEAF, NODE = b"\\x00", b"\\x01"   # RFC 6962 domain separation

def leaf_digest(content):
    return sha256(LEAF + content.encode()).hexdigest()

def node_digest(l, r):
    return sha256(NODE + bytes.fromhex(l)
                       + bytes.fromhex(r)).hexdigest()

def build_merkle_tree(leaf_digests):
    current, levels = leaf_digests, [leaf_digests]
    while len(current) > 1:
        current = [node_digest(current[i],
                     current[i+1] if i+1 < len(current)
                                  else current[i])
                   for i in range(0, len(current), 2)]
        levels.append(current)
    return current[0], levels
\end{lstlisting}

\subsection{SLSA v1.0 and in-toto Predicate Envelopes}
To integrate with cloud-native artifact registries and software supply chain attestors, EviAssure formats each generated \texttt{EvidenceBundle} into an in-toto v0.2 Statement envelope compatible with SLSA Provenance v1.0 specifications~\cite{torres2019in-toto, slsa2023supply}. The envelope carries \texttt{\_type} \texttt{https://in-toto.io/Statement/v0.1}, the artifact digest as \texttt{subject}, \texttt{predicateType} \texttt{https://slsa.dev/provenance/v1}, and the \texttt{evidence\_id} under \texttt{predicate.buildDefinition.externalParameters}; a complete example is in the artifact.

\section{Fail-Closed Policy Verification Engine}
Release authorization in EviAssure is governed by a zero-trust policy engine (\textnormal{\texttt{ReleasePolicyEngine}}) that enforces mandatory fail-closed default rules. This section details the declarative policy schema, multi-factor evaluation protocol, key governance rules, and the system-level formal security analysis.

\subsection{Declarative Policy Governance Rules}
A release policy specification $P$ defines strict quantitative safety thresholds and cryptographic verification conditions:
\begin{equation}
\begin{split}
P = (\,P_{\text{min\_pass}},\ &P_{\text{max\_drift}}, P_{\text{min\_sigs}}, P_{\text{valid\_window}},\\
&\mathcal{K}_{\text{trusted}}, \mathcal{K}_{\text{rev}}, \mathcal{A}_{\text{KMS}})
\end{split}
\end{equation}
where:
\begin{itemize}
    \item $P_{\text{min\_pass}}$: Minimum required unit and integration test pass rate (default: $100.0\%$ (100/100)).
    \item $P_{\text{max\_drift}}$: Maximum allowable unresolved security drift findings (default: $0$).
    \item $P_{\text{min\_sigs}}$: Required threshold number of valid signatures from authorized keys (default: $K=1$).
    \item $P_{\text{valid\_window}}$: Evidence timestamp freshness validity window $\Delta T_{\text{max}} = 3,600\text{ s}$ with future clock-skew tolerance $\delta_{\text{skew}} = 30\text{ s}$ (assuming RFC 5905 NTP synchronisation).
    \item $\mathcal{K}_{\text{trusted}}$: Trusted key registry pinning authorized key IDs to Ed25519 public keys (\nolinkurl{governance/trusted_keys.yaml}). Verification uses the pinned key, never one supplied inside a bundle; unregistered IDs are rejected. Authority thus rests on a small, versioned, offline-pinnable root set, following key transparency~\cite{melara2015coniks} and delegation hierarchies~\cite{kuppusamy2016tuf}.
    \item $\mathcal{K}_{\text{rev}}$: Key Revocation List (CRL) tracking compromised or deprecated key identifiers.
    \item $\mathcal{A}_{\text{KMS}}$: Required Cloud KMS or HSM key ARN boundary pattern matching.
\end{itemize}

Listing~\ref{lst:proto2} outlines the complete zero-trust verification algorithm enforced by \textnormal{\texttt{ReleasePolicyEngine}}.

\begin{lstlisting}[language=python,caption={Fail-Closed Policy Gate Verification Protocol.},label={lst:proto2}]
# In: Evidence E, Policy P, revoked K_rev, seen N_seen
# Out: D in {APPROVED, BLOCKED}; any exception => BLOCKED
def evaluate_release_gate(E, P, K_rev, N_seen):
    if N_seen is None: return BLOCKED  # no replay state
    valid = set()
    for s in E.signatures:
        if s.key_id in K_rev: continue
        if s.key_id not in K_trusted: continue   # registry
        pk = K_trusted[s.key_id]                 # pinned only:
        if s.pub_key and s.pub_key != pk: continue
        if not match_kms_arn(s.kms_arn, P.kms): continue
        if verify_ed25519(canon(E), s.sig, pk):
            valid.add(s.key_id)          # distinct signers
    if len(valid) < P.min_signatures:     return BLOCKED
    if merkle_root(E.traces) != E.merkle_root: return BLOCKED
    if E.traces_count != len(E.traces):   return BLOCKED
    if E.test_pass_pct < P.min_pass_pct:  return BLOCKED
    if E.unresolved_drift > P.max_drift:  return BLOCKED
    if E.nonce in N_seen:                 return BLOCKED
    if not fresh(E.timestamp, P.window, skew=30): return BLOCKED
    N_seen.add(E.nonce);  return APPROVED
\end{lstlisting}

\subsection{Formal Security Analysis}\label{sec:proofs}

The analysis has two layers. Theorem~\ref{thm:failclosed} is primitive-level: a game sequence reducing to EUF-CMA and collision resistance. Theorem~\ref{thm:authorization} is system-level: what an APPROVED verdict \emph{means}, and the trust boundaries --- collector, key registry, nonce state --- that meaning rests on.

\begin{theorem}[Fail-Closed Release Soundness]\label{thm:failclosed}

Under the assumption that Ed25519~\cite{bernstein2012high} is EUF-CMA secure~\cite{goldwasser1988digital, brendel2021ed25519} and SHA-256 is collision-resistant, no probabilistic polynomial-time adversary $\mathcal{A}$ can induce \textnormal{\texttt{ReleasePolicyEngine}} to output $\text{APPROVED}$ for a modified trace history, forged signature, replayed nonce, or non-compliant evidence payload except with negligible probability $\text{negl}(\lambda)$.
\end{theorem}

\begin{proof}
Two hops carry content. $\mathbf{Game}_1$ aborts if $\mathcal{A}$ produces a valid signature over a payload the honest packager never issued; an EUF-CMA adversary $\mathcal{B}$ installs its challenge key as the sole registry entry and answers packaging requests through its signing oracle on $\mathrm{canon}(E)$, so the simulation is perfect and a $\text{Game}_1$ abort is a forgery on a never-queried message. $\mathbf{Game}_2$ aborts on $\mathcal{T}^*\neq\mathcal{T}$ with equal root; under domain-separated leaves and the committed depth this needs a SHA-256 collision. Replay, freshness, revocation, registry membership and threshold comparison are table lookups over the signed payload, rejecting with probability $1$ given correct gate state, and contribute no term. Hence $\mathrm{Adv}^{\text{tamper}} \le \mathrm{Adv}^{euf\text{-}cma}_{\mathrm{Ed25519}}(\mathcal{B},t,q_s) + \mathrm{Adv}^{coll}_{\mathrm{SHA256}}(\mathcal{C},t)$, read at the 128-bit level rather than asymptotically in $\lambda$, since both primitives are fixed-parameter.
\end{proof}

Theorem~\ref{thm:failclosed} bounds an adversary's ability to \emph{fake} evidence; it says nothing on its own about what a genuine, honestly-signed evidence pack entitles its holder to. That is the release-gate question, and it is Theorem~\ref{thm:authorization}.

\begin{theorem}[Evidence-Bound Release Authorization Soundness]\label{thm:authorization}
Fix a policy version $P$ and a trusted key registry $\mathcal{K}_{\text{trusted}}$. If \textnormal{\texttt{ReleasePolicyEngine}} outputs $\text{APPROVED}$ for evidence $E$, then, except with probability $\text{negl}(\lambda)$, all of the following hold:
\begin{enumerate}
    \item[(i)] \textbf{Authenticity:} $E$ carries a valid Ed25519 signature by a non-revoked key pinned in $\mathcal{K}_{\text{trusted}}$; attacker-generated keys, spoofed key IDs, and revoked keys cannot produce a valid signature.
    \item[(ii)] \textbf{Evidence binding:} the recomputed Merkle root over the submitted trace set equals the signed root, the signed trace count equals the number of submitted traces, and every inclusion proof binds to the committed tree depth under disjoint leaf/node domains. The trace set the gate evaluated is therefore \emph{the one the collector signed} --- no trace was added, removed, reordered or edited between collection and adjudication. This says nothing about the relation between the signed trace set and the underlying execution; that gap is Definition~\ref{def:completeness} and is not closed by any cryptographic check.
    \item[(iii)] \textbf{Freshness:} the timestamp lies within the validity window $\Delta T_{\text{max}}$ and the skew bound $\delta_{\text{skew}}$, and the nonce has not been processed within the window; the release is not a replay of an earlier approved bundle.
    \item[(iv)] \textbf{Policy compliance:} all declared thresholds (test pass rate, unresolved drift, KMS ARN boundary, signature threshold $K$) satisfy policy version $P$; an APPROVED verdict under a different policy version is not an APPROVED verdict for this release.
\end{enumerate}
\end{theorem}

\paragraph{Scope and Operational Assumptions.}
The guarantees hold conditionally on (a) the \emph{collector assumption} of Definition~\ref{def:completeness} --- the evidence collector observed and recorded every action of the release execution --- which supplies the completeness half of the guarantee, and (b) the \emph{key-management assumption} --- registry keys are honestly generated and the HSM/KMS enforces the ARN boundary. The theorem asserts nothing about actions that never reached the collector, nor about the semantic correctness of recorded trace content; both are defined in the threat model and handled by the out-of-band semantic inspection layer.

\begin{proof}
\textbf{(i)} Follows from Theorem~\ref{thm:failclosed}, Game$_1$, plus the deterministic registry checks enforced before any signature verification (\texttt{policy.py}): unknown key IDs, bundle-supplied keys disagreeing with the registry, and revoked keys are rejected outright, so per-signature success is bounded by $\text{Adv}_{\text{Ed25519}}^{\text{euf-cma}}(\mathcal{A})$. \textbf{(ii)} Follows from Theorem~\ref{thm:failclosed}, Game$_2$, plus the deterministic signed-count check $N = |\text{traces}|$; substitution requires either a hash collision or a broken signature. \textbf{(iii)} Freshness, skew, and nonce checks are deterministic (Game$_3$; Listing~\ref{lst:proto2}) and hold with probability $1$ given correct gate state, with the disclosed caveat that replay protection requires sharing the nonce cache across gate replicas in distributed environments. \textbf{(iv)} Threshold comparisons are deterministic over the signed payload and the pinned policy version. Composition yields the statement; the only probabilistic terms reduce to the primitive advantages of Theorem~\ref{thm:failclosed}.
\end{proof}

\section{Deployment Integration and Workload Specimens}
EviAssure runs as a GitHub Action against two synthetic workload specimens --- a backend multi-step agent (\nolinkurl{specimens/agent_runner.py}: JWT claims, vector retrieval, sandboxed SQL) and a browser agent (\nolinkurl{specimens/web_app_runner.py}: navigation, clicks, DOM and screenshot digests) --- both simulations, and a third (\nolinkurl{specimens/live_agent_runner.py}) that drives a real tool-calling LLM through witnessed tools (Section~\ref{sec:live}). The production collector of Section~\ref{sec:system_threat} (eBPF syscall mediation) remains an architectural proposal rather than an evaluated component, which is why Section~\ref{sec:witness} moves the completeness guarantee onto witnesses a compromised collector cannot speak for.

\subsection{CI/CD Integration}
The gate ships as a GitHub Action that loads the pinned key and witness registries and the policy, ingests the \texttt{EvidenceBundle} together with the orchestrator's release request naming the credentialed session, runs \texttt{ReleasePolicyEngine} (\nolinkurl{scripts/verify_release_gate.py}), and on failure emits structured diagnostics and exits non-zero on every output path. The shipped policy requires witnessed completeness: an unwitnessed bundle, or one whose session is not the credentialed one, is BLOCKED, and the shipped registry (\nolinkurl{governance/witness_registry.yaml}, written by \nolinkurl{scripts/provision_witnesses.py}) carries the demo witnesses and orchestrator key so that the packaging script and the CLI exercise the full witnessed path end to end; the CLI-level omission tests run against exactly that configuration.

\subsection{Agent Execution Trace Corpus (\texorpdfstring{$N=\corpusProfiles$}{N=1,075})}\label{sec:corpus}
The corpus (\nolinkurl{corpus/agent_trace_corpus.json}) contains \corpusProfiles{} profiles / \corpusRecords{} trace records across five agent architectures, four to six steps each: \corpusClean{} clean, \corpusOvert{} \emph{overt} anomalies over ten classes, and \corpusStealth{} \emph{stealth} anomalies over five classes. It is entirely synthetic and templated, as its \texttt{provenance} field states (Section~\ref{sec:limitations}).

The overt/stealth split guards against a benchmark that grades itself. If every planted anomaly used an action name and status absent from clean traffic and the detector keyed off a list of exactly those names, generator and detector would be one list and the reported recall a property of the generator. \emph{Overt} classes have that structure (out-of-vocabulary action and status) and are the easy half. \emph{Stealth} classes are the point: a mutating statement issued through the read-only query path, a ledger transfer to an attacker-controlled destination, retrieval returning injected context, extraction used as an exfiltration channel, a linter invoked with findings suppressed. Each uses an action, a status and a duration drawn from clean traffic, so no per-leaf vocabulary or envelope test can separate them. Section~\ref{sec:twolayer} reports both halves separately.
\section{Systems Security Evaluation}\label{sec:eval}
The empirical evaluation follows a deliberate two-part strategy: a micro-level validation of the gate's enforcement mechanisms, and a macro-level evaluation of the system deployed at corpus scale.

\textbf{Mechanism evaluation.} Baseline performance is measured through Merkle scaling latency and multi-core verifier throughput. Adversarial resilience is measured against the vector suite: twelve vectors derived from the five capability classes ($\mathcal{A}_1$--$\mathcal{A}_5$), each exercising one enforcement, plus five authored against the design rather than against the check list --- the ones with the power to fail, and three of which found defects during development. Clean negative controls measure the false-block rate, without which a block rate is uninterpretable. The bundles on this axis carry no witness attestations by construction, so the gate is evaluated here with witness reconciliation disabled (the \emph{unwitnessed evaluation profile}, \texttt{witnessed=False}, recorded in the artifact); the omission suite (Section~\ref{sec:omission}) tests reconciliation itself, the axis the tamper vectors structurally cannot reach, and the shipped policy requires it.

\textbf{System-level deployment.} A corpus-scale evaluation over \corpusProfiles{} agent trace profiles exercises the interaction between the cryptographic integrity gate and an out-of-band semantic inspection layer --- and, importantly, shows where that interaction still leaves a gap.

Results are split by kind. \emph{Deterministic} security results (block counts, ablation, inspection) are produced by \nolinkurl{scripts/run_security_eval.py} into \nolinkurl{results/security_evaluation.json} and reproduce identically on any machine. \emph{Timed} measurements are wall-clock timings recorded by \nolinkurl{scripts/run_release_benchmark.py} into \nolinkurl{results/benchmark_summary.json} on an Apple M4 Max (14 cores, no SMT) running Python 3.14.x; each is the mean over \benchRepeats{} runs, with the standard deviation recorded alongside in the artifact. Verdicts are deterministic checks evaluated once, so run-to-run dispersion is zero by construction; sampling uncertainty over the finite vector set is reported instead, as a Wilson 95\% interval on every rate.

\subsection{Merkle Scaling up to \texorpdfstring{$N=1,000,000$}{N=1,000,000} Traces}
Merkle build time was evaluated from $N=10$ to $N=10^6$ over \benchRepeats{} runs per point (Figure~\ref{fig:scaling}). Construction scales to $10^6$ traces in $\merkleBuildOneMMs\text{ ms}$ (mean; $\sigma=\merkleBuildOneMStd\text{ ms}$ across runs), and to $\merkleBuildMs\text{ ms}$ for $N=100,000$. We do not report a ``packaging overhead'' ratio: post-tree bundle construction and signing cost a constant independent of $N$, and dividing a constant by an assumed per-step execution time that grows with $N$ produces a curve that falls for arithmetic reasons rather than measuring any property of the system. Merkle construction, the component that actually scales, is what is reported.


\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/merkle_scaling.png}
\caption{Merkle construction and envelope packaging from $N=10$ to $N=10^6$,
mean over \benchRepeats{} runs with the standard deviation as a band. Both axes
are logarithmic. The callout is read from
\nolinkurl{results/benchmark_summary.json} rather than typed, so it cannot
diverge from \merkleBuildOneMMs{}~ms in the text.}
\label{fig:scaling}
\end{figure}

\subsection{Parallel Verifier Throughput}
Verifier throughput (Figure~\ref{fig:throughput}) was evaluated using a Python \texttt{ProcessPoolExecutor} across 1, 2, 4, 8, and 16 worker cores over 1,000 evaluation requests per run and \benchRepeats{} runs per configuration; each evaluation is a distinct fresh-nonce bundle against a warm policy engine (evidence creation and replay-state checks are measured separately, so the timed region is steady-state verification). Throughput rises from \singleWorkerThroughput{} ops/s on a single worker to a peak of \peakThroughput{} $\pm$ \peakThroughputStd{} operations/second (mean $\pm$ std) at \peakWorkers{} workers (a $\peakRatio\times$ speedup), then declines; each task is a three-trace bundle shipped to a worker process, so the measurement is dominated by inter-process serialization rather than verification, and we do not attribute the decline to hardware. The figure characterises \emph{small} bundles: because the gate recomputes the whole tree (Listing~\ref{lst:proto2}), per-bundle verification cost is linear in $N$, and at $N=10^6$ the gate is bounded by the $\merkleBuildOneMMs\text{ ms}$ rebuild, not by this number.


\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/parallel_throughput.png}
\caption{Verifier throughput against process-pool size, mean $\pm$ standard
deviation over \benchRepeats{} runs of 1{,}000 evaluations. The peak bar and its
ratio are selected from the data, not fixed to a worker count. Throughput is
measured on three-trace bundles and does not describe large-$N$ verification.}
\label{fig:throughput}
\end{figure}

\subsection{Sparse Inclusion Proofs}
Auditors verifying individual high-risk steps need not re-process full logs (\rawSizeOneMKb{}\,KB at $N=10^6$): at that size the committed depth is $d=20$, proof generation takes $\sparseProofGenMs\text{ ms}$, and the \sparseProofSizeKb{}\,KB payload of \sparseProofNodes{} node digests verifies in $\sparseProofVerifyMs\text{ ms}$.

\subsection{Adversarial Vector Suite}\label{sec:tamper}
\begin{table}[t]
\centering
\caption{Adversarial vectors and gate outcomes (unwitnessed evaluation profile). V1--V12 target one enforcement each; V14--V18 were authored against the design, independently of the check list; $\dagger$ marks the three that found a defect during development. V13 is retained as a configuration check but is not scored (Section~\ref{sec:limitations}).}
\label{tab:tamper}
\footnotesize
\setlength{\tabcolsep}{3pt}
\begin{tabular}{@{}lp{2.9cm}ll@{}}
\toprule
\textbf{ID} & \textbf{Vector} & \textbf{Class} & \textbf{Outcome} \\
\midrule
\multicolumn{4}{@{}l}{\emph{Mechanism vectors (one per enforcement)}}\\
V1 & Unsigned payload & $\mathcal{A}_2$ & Blocked \\
V2 & Tampered trace digest & $\mathcal{A}_1$ & Blocked \\
V3 & Attacker-key signature & $\mathcal{A}_2$ & Blocked \\
V4 & Replayed nonce (same gate) & $\mathcal{A}_3$ & Blocked \\
V5 & Expired timestamp & $\mathcal{A}_3$ & Blocked \\
V6 & Unresolved drift & policy & Blocked \\
V7 & Sub-threshold pass rate & policy & Blocked \\
V8 & Forged Merkle root & $\mathcal{A}_1$ & Blocked \\
V9 & Revoked key ID & $\mathcal{A}_4$ & Blocked \\
V10 & Duplicate-key wire doc & $\mathcal{A}_5$ & Blocked \\
V11 & Future clock skew & $\mathcal{A}_3$ & Blocked \\
V12 & Trace-set truncation & $\mathcal{A}_1$ & Blocked \\
\midrule
\multicolumn{4}{@{}l}{\emph{Independent vectors (authored against the design)}}\\
V14 & Attacker key, registry off & $\mathcal{A}_2$ & Blocked$^\dagger$ \\
V15 & Internal node as leaf & $\mathcal{A}_1$ & Blocked$^\dagger$ \\
V16 & Cross-replica nonce replay & $\mathcal{A}_3$ & \textbf{Not blocked} \\
V17 & 64-char leaf confusion & $\mathcal{A}_1$ & Blocked$^\dagger$ \\
V18 & Post-signing DOM mutation & $\mathcal{A}_1$ & Blocked \\
\midrule
\multicolumn{2}{@{}l}{Scored total} & \multicolumn{2}{l@{}}{\eviBlocked/\eviTotal\ (\eviPct\%)}\\
\multicolumn{2}{@{}l}{Clean negative controls} & \multicolumn{2}{l@{}}{\falseBlockK/\falseBlockN\ falsely blocked}\\
\bottomrule
\end{tabular}
\end{table}

EviAssure blocks \eviBlocked{} of \eviTotal{} scored vectors (\eviPct\%, 95\% CI [\eviCIlo, \eviCIhi]), and falsely blocks \falseBlockK{} of \falseBlockN{} clean negative controls. Three points about how that number should be read.

\textbf{The mechanism vectors do not demonstrate resilience.} V1--V12 map close to one-to-one onto the gate's checks, because we wrote both; they are a completeness test for the implementation and nothing more, and V14 and V17 --- neither reachable from the check list --- show why a 1:1 map proves nothing about the design.

\textbf{Three vectors ($\dagger$) found real defects during development.} V14: a gate that falls back to the key carried \emph{inside the bundle} when the registry is disabled approves an adversary's own keypair with a conformant ARN. V17: a length heuristic lets a 64-character leaf string be adopted as its own digest. V15: the CVE-2012-2459-family internal-node substitution against the auditor path. Each was found by writing the vector and is now blocked and regression-tested, which is the argument for authoring vectors against the design.

\textbf{V16 is not blocked, and we report it.} The same approved bundle presented to a second gate replica that does not share nonce state is APPROVED, because replay protection is a property of gate \emph{state}, not of the evidence. The shipped verifier persists nonces and fails closed when the ledger is unavailable, but cross-replica correctness needs shared storage the prototype does not provide.

\textbf{Differential wire fuzzing.} A fuzzing campaign whose every mutation perturbs a field inside the signed payload cannot fail --- a verifying bundle is unreachable and no clean case exists --- so it carries no information. Ours perturbs the \emph{serialization} of a validly signed bundle --- key order, insignificant whitespace, unicode escaping, number formatting, duplicate keys, byte-order marks --- where both outcomes are reachable. Over \fuzzCases{} cases (20\% clean controls): \fuzzBlockedK/\fuzzBlockedN{} semantics-changing encodings blocked, \fuzzApprovedK/\fuzzApprovedN{} semantics-preserving encodings approved. The campaign found a real defect during development: a duplicate-key detector that accumulated key counts across every object in the document registered sibling objects in the \texttt{traces} array as duplicates, so any deployment supplying the raw wire text rejected every multi-trace bundle. It is fixed and regression-tested.

\subsection{Per-Check Ablation}
Each enforcement was disabled in turn and the suite re-evaluated (matrix in the artifact's \texttt{ablation} block). Disabling the signature check releases V1, V3, V9, V14 and V18; removing Merkle re-derivation releases V2 and V8; the quality, drift, freshness and replay checks each release exactly the vectors they target (V7; V6; V5 and V11; V4). V16 escapes every variant, including the full gate.

Two results are worth stating precisely. First, \textbf{not every check is necessary against this suite}: disabling the signed trace-count binding releases no vector, because V12 is independently caught by root recomputation; the count binding earns its place by protecting the \emph{auditor} path, whose proof depth is derived from the signed count. Second, \textbf{emptying the trusted-key registry collapses the gate rather than weakening it}: all Ed25519 evidence is rejected, which is only true of an implementation that never falls back to a bundle-supplied key --- V14 checks it.

\subsection{Omission Attacks}\label{sec:omission}
The tamper suite and the omission suite test different things. A tamper vector modifies evidence and is caught by an integrity check. An omission vector modifies \emph{nothing}: the adversary performs an action and declines to record it, or performs it in a session the gate never sees, then signs honestly over what it presents. There is no discrepancy to detect, which is why Theorem~\ref{thm:separation} rules the class out for document verifiers rather than merely predicting they will do badly at it.

We construct seven (Table~\ref{tab:omission}) plus an honest fully-witnessed control that every system must approve --- without which a reconciler that rejects everything would score perfectly. O1--O4 truncate: an interior action, the trailing action, every action of one witness, and the trailing action with a forged lower closing. O5 splices receipts from a second credentialed session into the gap. O6 fabricates a mediated action no witness served. O7 performs the dangerous action in one credentialed session and submits the complete, honestly witnessed bundle of a second, benign credentialed session for the release. Every session is opened by an orchestrator credential; a request that carries none, or a credential signed by anyone else, is refused by the witness before any bundle exists, and that refusal is exercised by a test rather than scored as a vector.

\begin{table}[t]
\centering
\caption{Omission attacks detected, out of \omVectors{}. All systems approve the honest control (OC1). in-toto/DSSE and TUF are executed verifiers; the receipt and chaining verifiers are re-implemented from their published designs and run on the same records (Section~\ref{sec:witness}); every row is executed, none is a presence check.}
\label{tab:omission}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lp{3.6cm}c@{}}
\toprule
& \textbf{Verification approach} & \textbf{Detected} \\
\midrule
in-toto/DSSE & signature over the document & \omDsse/\omVectors \\
TUF & expiry, thresholds, revocation & \omTuf/\omVectors \\
AAS-1 style & per-issuer hash chaining & \omChain/\omVectors \\
Receipts (2026) & per-action receiver attestation~\cite{notarized2026agents, pipelab2026receipts} & \omReceipts/\omVectors \\
EviAssure & full gate, no reconciliation & \omNoWtc/\omVectors \\
\textbf{EviAssure} & \textbf{+ witnessed completeness} & \textbf{\omWtc/\omVectors} \\
\bottomrule
\end{tabular}
\end{table}

Three results are worth reading carefully. First, \textbf{EviAssure without reconciliation detects \omNoWtc{} of \omVectors{}} despite enforcing signatures, Merkle re-derivation, count binding, freshness and replay. The mechanism, not some incidental check, is what closes the class.

Second, \textbf{per-action receipts detect \omReceipts{} of \omVectors{}}. Receipts catch the two attacks that leave a trace \emph{without} a matching attestation --- cross-session splicing and fabrication --- and miss all four truncations, because dropping an action drops its receipt and the remaining pairs still match; they also miss substitution, because the substituted bundle's receipts are perfectly matched to its own session. This is the empirical form of the paper's central observation: a receipt proves an action happened; a set of receipts does not prove no other action happened, nor that it happened in the session under evaluation.

Third, \textbf{hash chaining detects \omChain{} of \omVectors{}}. A chain links records that are present, so an interior gap breaks it and a spliced foreign receipt fails to link, but a suffix truncation does not --- the end simply moves --- and neither does dropping a whole issuer, forging a closing the chain never consults, fabricating an unchained trace, or substituting a session whose chain is intact. This is the precise limitation of AAS-1's omission story, and it is why the closing count and the credentialed session, not the chain, are what make truncation and substitution detectable.

\begin{figure}[t]
\centering
\includegraphics[width=\columnwidth]{figures/comparative_block_rate.png}
\caption{Executed baselines against the \scoredVectors{}-vector suite, with Wilson 95\%
intervals. The intervals overlap, and that is the point: at this sample
size the evaluation supports a qualitative account of \emph{which} vectors
survive a modern composed pipeline, not a strong quantitative separation
(Section~\ref{sec:comparative}).}
\label{fig:comparative}
\end{figure}

\subsection{Comparative Gate Analysis}\label{sec:comparative}
Each baseline is configured as a competent engineer would deploy it, and executed; counts and intervals are in Figure~\ref{fig:comparative}. \textbf{in-toto/DSSE} (DSSE v1 pre-authentication encoding + Ed25519 verification) authenticates the envelope against a \emph{pinned trust root} rather than a key travelling with the payload. \textbf{TUF} evaluates Timestamp-role expiry with \texttt{python-tuf}'s metadata objects over that trust-root check, so it supplies \texttt{expires}, a signature threshold and root-driven revocation. \textbf{OPA Rego} (\nolinkurl{governance/baseline_opa.rego}) is executed by the \texttt{opa} binary; the evaluation script's \texttt{--require-executed} flag, and the artifact packager, refuse to record or ship a modelled substitute for any of these three. The \textbf{composed pipeline} is DSSE $\wedge$ TUF $\wedge$ OPA. The \textbf{unauthenticated status gate} is retained as a \emph{floor}, not a competitor.

The honest headline is therefore \eviBlocked/\eviTotal{} versus \compBlocked/\compTotal{} (Figure~\ref{fig:comparative}), and the intervals overlap: with \eviTotal{} vectors this evaluation cannot support a strong quantitative separation, and we do not claim one. What it does support is a \emph{qualitative} account of which vectors survive a modern composed pipeline and why. The composed baseline blocks every authentication, freshness, revocation and policy-threshold vector; TUF alone blocks \tufBlocked{}, since freshness, revocation and threshold signing were solved in 2010~\cite{samuel2010surviving}.

The vectors it does not block are the ones this paper is actually about: V2 and V8 (trace-level integrity --- no baseline recomputes a Merkle root over an action history), V12 (trace-set truncation), V15 and V17 (proof-path and leaf-typing attacks on the audit surface), and V4 (within-gate replay, which requires the verifier to hold state across submissions rather than to validate a document). That is the delta: \emph{binding the release decision to the execution trace, and holding state while deciding}. It is a narrow claim, and it is one the evaluation can carry.

\subsection{Corpus-Scale Layered Evaluation}\label{sec:twolayer}
Neither the tamper nor the omission suite reaches the semantic axis: a complete, witnessed, correctly signed bundle can still record behaviour nobody authorised. The corpus measures that third layer (Table~\ref{tab:corpus}).

\begin{table}[t]
\centering
\caption{Layered evaluation over \corpusProfiles{} profiles / \corpusRecords{} trace records. L3 uses a detector fitted on clean profiles only.}
\label{tab:corpus}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lp{3.1cm}cc@{}}
\toprule
\textbf{Layer} & \textbf{Check} & \textbf{Result} & \textbf{Rate} \\
\midrule
L1 Integrity & Merkle root consistency & \corpusProfiles/\corpusProfiles & 100\% \\
L1 Integrity & Depth-bound proofs valid & \corpusProfiles/\corpusProfiles & 100\% \\
L2 Gate & Clean APPROVED & \corpusClean/\corpusClean & 100\% \\
L2 Gate & Anomalous APPROVED & \insRecallN/\insRecallN & 100\% \\
L3 Inspect & Overt recall & \insOvertK/\insOvertN & 100\% \\
L3 Inspect & \textbf{Stealth recall} & \textbf{\insStealthK/\insStealthN} & \textbf{0\%} \\
L3 Inspect & Overall recall & \insRecallK/\insRecallN & \insRecallPct\% \\
L3 Inspect & Clean falsely flagged & \insFPK/\insFPN & 0\% \\
\bottomrule
\end{tabular}
\end{table}

L1 and L2 are as before: all bundles are Merkle-consistent with valid depth-bound proofs, all clean profiles are APPROVED, and \emph{all} anomalous profiles are also APPROVED. We report the latter rather than hiding it --- semantic misbehaviour that is cryptographically well-formed is outside the gate's scope by design.

L3 is a held-out protocol. The detector consults no list of anomalous actions. It is fitted on clean profiles alone, learning an action vocabulary, a status vocabulary and a per-action duration envelope, and every anomaly class is held out from that fit. Under this protocol it recalls \insOvertK/\insOvertN{} overt anomalies and \insStealthK/\insStealthN{} stealth anomalies --- overall \insRecallK/\insRecallN{} (\insRecallPct\%, CI [\insRecallCIlo, \insRecallCIhi]) at \insFPK/\insFPN{} false positives.

The zero is the result. Per-leaf inspection detects agents that do things they have never done before; it is blind to an agent doing a permitted thing for an impermissible reason, which is precisely what a prompt-injected agent looks like in a trace. Combined with L2, the layered picture is: the gate proves what was recorded is genuine, inspection catches structural novelty, and \emph{neither} catches semantically disguised misbehaviour.

\subsection{Live Agent Sessions}\label{sec:live}
Everything above is computed over a templated corpus, which leaves the paper's
own premise untested: does witnessed completeness survive an agent that actually
decides what to do? We therefore ran \liveSessions{} sessions of a real
tool-calling LLMs (\liveModelCount{} model families,
\liveProviderCount{} providers) against a small tool set fronted by
witnesses of Section~\ref{sec:witness}. The model chooses the actions; nothing
is scripted (Table~\ref{tab:live}). What the harness does and does not separate must be stated before
its numbers are read. Each witness runs outside the collector: it executes the
tool, digests the output \emph{it} produced, and signs with a key never
exported from its own address space. Separation is by process, key and
credential --- and by container, with no network and a read-only repository,
in \liveIsoContainer{} sessions --- on one host: not by host or trust domain,
nor against root or a debugger.

\begin{table}[t]
\centering
\caption{Live agent sessions (\liveModel). Coverage is the share of the agent's
calls that went to the four witnessed tools; the fifth tool is unwitnessed by
design. It is split by how the session ended, because a run stopped by the turn
cap never reaches the unwitnessed call.}
\label{tab:live}
\footnotesize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{@{}lcc@{}}
\toprule
& \textbf{Ran to completion} & \textbf{Truncated} \\
\midrule
Sessions & \liveCompleted & \liveTruncated \\
Witness coverage & \textbf{\liveCoverageCompleted\%} & \liveCoverageTruncated\% \\
\midrule
\multicolumn{3}{@{}l}{\emph{Over all \liveSessions{} sessions}}\\
Distinct action sequences & \multicolumn{2}{c@{}}{\liveDistinctSeq/\liveSessions\ (\liveNondetPct\%)} \\
Honest control reconciles & \multicolumn{2}{c@{}}{\liveControlOk} \\
Omission attacks detected & \multicolumn{2}{c@{}}{\liveOmDetected/\liveOmTotal\ (\liveOmVectorsRun\ vectors)} \\
\bottomrule
\end{tabular}
\end{table}

The sequences are genuinely non-deterministic --- \liveDistinctSeq{} of
\liveSessions{} sessions were distinct (\liveNondetPct\%), over
\liveActionsMean{} $\pm$ \liveActionsStd{} actions --- and the agent generated
its own failures, malformed SQL refused by the read-only path (\texttt{DENIED})
and queries against non-existent tables (\texttt{ERROR}), rather than executing
a happy path we wrote for it.

\textbf{Reconciliation is sound on real traces}: the honest control reconciles
\liveControlOk{}, and re-deriving the omission attacks against each live session
detects \liveOmDetected/\liveOmTotal. That total covers
\liveOmVectorsRun{} of the \liveOmVectorsSuite{} vectors: \liveOmMissing{} need
a second session --- a concurrent one for the splice, a second credentialed one
for the substitution --- and were not constructed for the recorded runs. A
vector that cannot be constructed for a given session --- an interior omission
in a three-action run, say --- is dropped rather than scored, since counting an
attack that never ran as detected is how a suite flatters itself. Detection is
cryptographic, so this confirms rather than surprises; but a protocol that
failed here would have been refuted, and it was not.

\textbf{Witness coverage is a property of the tool set, and it was
\liveCoverageCompleted\%.} Four of the five tools are witnessed and
\texttt{\liveUnmediated} is not, so coverage is the share of the agent's calls
that went to the four; the pooled figure over all \liveSessions{} sessions ---
\liveCoverageMean\% --- overstates it, because only \liveCompleted{} sessions
ran to completion and a session stopped by the turn cap never reaches the
unwitnessed call, giving \liveCoverageTruncated\% by construction of the cap.
The number reports the residue Definition~\ref{def:wtc} leaves open for this
deployment --- roughly one action in five of a completed session lay outside
the guarantee --- and its value is that it is declared and measurable rather
than assumed away, not that it is a discovery about agents. \liveFailures{} of
\liveRequested{} sessions was lost to a provider-side output-parsing error,
which we report rather than retry into silence.

\subsection{Limitations and Threats to Validity}\label{sec:limitations}
\textbf{The corpus is synthetic and the live harness is small.} Section~\ref{sec:live} runs a real tool-calling LLM against the witness protocol, so the completeness claim is not evaluated only on templates. The \corpusProfiles-profile corpus behind Section~\ref{sec:twolayer} and the semantic-inspection results is synthetic and templated, as the artifact's own \texttt{provenance} field declares. The live sessions are modest in scale (\liveSessions{}, of which \liveCompleted{} ran to completion), \liveProviderCount{}-provider, use benign tools, and separate witness from collector only as far as Section~\ref{sec:live} states; they establish that the protocol composes with a real agent, not how it behaves under an adversarial one, and their coverage figure describes the tool set rather than agents in general.

\textbf{The collector is unimplemented.} Every security guarantee is conditioned on complete mediation at the collection boundary (Definition~\ref{def:completeness}). The production design grounds this at the OS boundary via eBPF; the evaluation uses Python instrumentation wrappers. The load-bearing component is therefore an architectural proposal, not a measured one.

\textbf{The gate does not block semantic misbehaviour, and inspection misses the hard half.} L2 approves all \insRecallN{} anomalous profiles by design; held-out L3 inspection recalls \insStealthK/\insStealthN{} stealth anomalies (Section~\ref{sec:twolayer}). Content-level policy over trace semantics is future work.

\textbf{V16 is unblocked.} Replay protection is a property of gate state. The shipped verifier persists nonces and fails closed when the ledger is unavailable, but cross-replica correctness requires shared storage (Redis/DynamoDB) that the prototype does not implement.

\textbf{Baseline scope.} DSSE/in-toto and TUF are executed verifiers and OPA is executed by its binary; the receipt and hash-chain baselines of Section~\ref{sec:omission} are our re-implementations of published designs, not their reference code, and are labelled as such in the artifact; the unauthenticated status gate is modeled and reported as a floor, not a competitor.

\textbf{Witness and orchestrator trust, and availability.} Witnessed completeness is exactly as strong as the declared witness set. An adversary holding a witness key mints valid receipts and closings for that witness; one holding the orchestrator's key mints credentials and can relabel or substitute sessions; and action types no witness mediates lie outside the guarantee by construction (Definition~\ref{def:wtc}). We evaluate neither witness or orchestrator compromise nor key rotation, and a witness that fails to close a session blocks the release --- fail-closed, but a denial-of-service surface a deployment must plan for. The gate must be told the credentialed session by the orchestrator, out of band; a deployment that lets the bundle nominate its own session has no defence against O7.

\textbf{Vector-set construct validity.} V1--V12 were authored alongside the checks they exercise and demonstrate only that those checks fire. V14--V18 were authored against the design and three of them found defects during development, which is evidence that the suite has some power --- but a \eviTotal{}-vector suite gives wide intervals (CI [\eviCIlo, \eviCIhi]), the \omVectors{}-vector omission suite mirrors the case analysis of Theorem~\ref{thm:wtc}, and no suite authored by the system's designers can establish resilience to novel attack.

\textbf{Measurement and key management.} Timed figures are means over \benchRepeats{} runs on one platform. The artifact signs with static demo keys so results reproduce; there is no real KMS round trip, so no KMS latency is measured or claimed.


\section{Related Work}

\textbf{Positioning.} Threshold signing, metadata expiry, rollback protection and key revocation are \emph{not} novel here --- TUF has supplied them since 2010~\cite{samuel2010surviving}, which is why our executed TUF baseline blocks \tufBlocked{} of \tufTotal{} vectors (Section~\ref{sec:comparative}). EviAssure's claim is narrow: it binds the release decision to a Merkle-attested \emph{execution trace} for a credentialed session, reconciles it against a second trust domain, and holds replay state, which none of the systems below does.

\subsection{Supply Chain Provenance}
in-toto~\cite{torres2019in-toto}, SLSA~\cite{slsa2023supply} and Sigstore/Cosign~\cite{sigstore2022cosign} provide cryptographic provenance for build pipelines and image digests, including vetted runtime-trace predicates~\cite{intoto_runtime_trace}; any release pipeline should use them for the static artifact side. What they do not provide is a stateful authorization layer --- freshness and replay are not integral to step attestations, and release decisions are not bound to attested traces under a pinned policy version --- and EviAssure supplies that layer while remaining envelope-compatible.

\subsection{Policy Engines and Workload Identity}
Policy engines (OPA~\cite{opa2021openpolicy}, Kyverno~\cite{kyverno2022policy}) evaluate declarative rules over admission payloads but compute no Merkle root over an action history and hold no replay state; workload identity frameworks (SPIFFE/SPIRE~\cite{spiffe2020spire}, Macaroons~\cite{birgisson2014macaroons}) establish \emph{who} the runner is, not what it did. Update frameworks are the closest prior art: TUF~\cite{samuel2010surviving} and its delegation extensions~\cite{kuppusamy2016tuf, moore2023artemis} supply expiry, thresholds, rollback and revocation, which is why we execute TUF as a baseline rather than claim those properties.

\subsection{AI-Agent Provenance Standards and Receipts}\label{sec:related-2026}
Three 2026 initiatives address agent provenance as a \emph{record format}. The in-toto \texttt{agent-decision/v0.1} RFC~\cite{intoto2026agent-decision} standardises the shape of a signed policy-decision record, leaving freshness, replay and revocation to the verifier. APAS~\cite{apas2026agent-provenance} builds provenance chains on in-toto Statements and grades itself candidly: at its lower levels provenance is ``self-attested --- an attacker who compromises the orchestrator can forge records.'' AAS-1~\cite{aas12026agent-audit} defines evidentiary record classes with per-issuer hash chains that detect omission \emph{between} records a verifier already holds --- which, as Section~\ref{sec:omission} measures, a suffix truncation evades.

The closest designs are receiver-attested receipts. Notarized Agents~\cite{notarized2026agents} has the service that serves an agent call sign a receipt binding the call's input and output digests, encrypt it to the agent's owner and publish it to a transparency log, and names what it does not solve: the ``suppression attack'', in which an agent that does not call a service produces no receipt, and set-completeness of the receipts a log returns. PipeLab's action receipts~\cite{pipelab2026receipts} are signed by a mediator outside the agent process and hash-chained. Both give per-receipt verifiability from a second trust domain, which is the same move as ours; neither gives a verifier a way to know that the receipt set is \emph{all} of them. Publishing receipts to a transparency service in the SCITT model~\cite{rfc9943scitt} or a Certificate-Transparency-style log~\cite{laurie2014certificate} is one route to that: a verifier that audits the log for every receipt in a session obtains completeness relative to the log, at the cost of a log operator in the trust domain and a query whose own completeness must be authenticated. The closing count is the other: each witness commits, under its own key, to how many actions it served, so completeness relative to the witness set needs no log and no cross-witness agreement, and the credentialed session pins which set the gate reconciles. The two compose --- receipts and closings can be logged --- and we regard the log as the natural place to keep closings for post-hoc audit.

Theorem~\ref{thm:separation} explains why record formats and receipt sets alone are structurally short of completeness: each verifies a submitted document. Their records ride inside EviAssure's envelope unmodified; what it adds is the state, the second trust domain and the session binding a document cannot carry.

\section{Ethics and Conclusion}
All datasets, trace profiles and harnesses are synthetic and locally generated: no human subjects, no customer PII, no live credentials, and no measurement touching third-party systems; dual-use considerations are analysed in Appendix~\ref{sec:appendix_ethics}.

EviAssure binds a release decision to a domain-separated, depth-bound Merkle attestation of an agent's execution trace, evaluated by a stateful fail-closed gate. Its central claim is Witnessed Trace Completeness: reconciling collector evidence against signed witness receipts and closing counts, for the session the orchestrator credentialed, detects \omWtc/\omVectors{} omission attacks every record-format baseline misses --- a class Theorem~\ref{thm:separation} shows no stateless verifier can close. On the tamper axis it blocks \eviBlocked/\eviTotal{} against \compBlocked{} for a composed in-toto/DSSE + TUF + OPA pipeline and reports the one it fails; held-out inspection recalls \insOvertK/\insOvertN{} overt and \insStealthK/\insStealthN{} disguised anomalies. Evidence-bound release assurance makes an unauthorised release undeniable and attributable, not impossible.

\ifanonymous\else
\section*{Acknowledgements}
The author is a researcher at The Alan Turing Institute.
\fi

\bibliographystyle{plain}
\bibliography{references}

\appendix
\section*{Use of Generative AI and Automated Tooling}
The authors disclose the use of AI coding assistants for manuscript drafting assistance, LaTeX typography checking, test-harness refactoring, and an automated build-and-consistency pipeline that regenerates every numeral in this paper from the artifact's results files. All references were verified by the authors against primary sources (publisher records, arXiv, or the live specification pages). The core conceptual architecture, security proofs, empirical findings, and intellectual claims remain the original work of the authors, who take responsibility for all content.

\section{Ethics and Dual-Use Analysis}\label{sec:appendix_ethics}
The research presented in this paper introduces a defensive mechanism to ensure the security and provenance of autonomous agent releases. This work does not involve human subjects, does not analyze personally identifiable information, and does not conduct active measurements against production systems or networks operated by third parties. The \scoredVectors{} adversarial tamper vectors and the \corpusProfiles{}-profile corpus are fully synthetic and generated within isolated, local test environments for the sole purpose of evaluating the proposed cryptographic and policy enforcement mechanisms. 

\paragraph{Dual-Use Considerations.}
Cryptographic execution attestation mechanisms, if repurposed maliciously, could theoretically be employed to construct invasive employee monitoring frameworks or enforce rigid software lock-in. EviAssure mitigates these dual-use concerns by enforcing decentralized multi-signature threshold governance and open policy schemas (\nolinkurl{governance/release_policy.yaml}), and by keeping raw payloads out of evidence bundles entirely --- traces carry digests only. Salted HMAC blinding (\texttt{blind\_payload}) additionally unlinks the same recorded action across deployments, though as Section~\ref{sec:witness} states, it does not conceal payload equality within one deployment.

\paragraph{Data Privacy \& Parameter Blinding.}
Autonomous software agents frequently process confidential prompt texts, customer financial details, or proprietary source code snippets. To prevent public release audit logs from exposing sensitive data, EviAssure keeps raw payloads out of evidence bundles entirely---traces carry digests only---and the blinding protocol additionally re-keys those digests with a per-deployment salted HMAC ($H_{\text{blinded}}$), so the same recorded action is not linkable across environments. Within one environment the deterministic construction still reveals repetition (Section~\ref{sec:witness}). This preserves full Merkle tree inclusion auditability without storing proprietary inputs in the audit log.

\paragraph{Responsible Disclosure \& Safety Impact.}
EviAssure raises the cost of forging or replaying release evidence produced by a compromised runner. It does \emph{not} prevent prompt injection: an injected agent produces genuine actions that the collector honestly records and the gate correctly approves, and Section~\ref{sec:twolayer} measures this directly --- all \insRecallN{} anomalous corpus profiles, including the injection class, are APPROVED. The contribution is that such a release becomes \emph{undeniable and attributable} after the fact, not that it is prevented. An earlier version of this appendix claimed prevention; that claim contradicted our own Section~\ref{sec:twolayer} results and has been withdrawn.

\section{Forensics and Diagnostics}\label{sec:forensics}
For incident response after a release, an offline forensic utility (\nolinkurl{scripts/audit_trace_forensics.py}) reconstructs the Merkle tree from a serialized \texttt{EvidenceBundle}, verifies signatures against the pinned registry and revocation list, and isolates leaf-index anomalies. Presented with a tampered payload it reports the exact leaf position $i$ and the output-hash mismatch, emitting structured JSON for security operations centre (SOC) ingestion.

\section{Deployment Trade-offs}\label{sec:tradeoffs}
Two engineering consequences follow. \emph{Audit granularity}: since a depth-bound proof costs \sparseProofSizeKb{}\,KB against \rawSizeOneMKb{}\,KB of raw trace, organisations can archive payloads to cold storage and keep only proofs online. \emph{Replay state}: the gate must hold state across submissions, which V16 shows it cannot do across replicas without shared storage; a key-value store with atomic set-if-absent and TTL $\Delta T_{\text{max}}$ is the natural implementation, and it must fail closed when that store is unreachable rather than admit a split-brain replay. Neither deployment is measured here.

\section{Open Science Appendix}\label{sec:appendix_open_science}
The reproducibility materials consist of the complete EviAssure implementation (cryptographic attestation, blinding, held-out inspection, and policy verification modules), the \corpusProfiles{}-profile agent execution trace corpus, the \scoredVectors{}-vector tamper harness with negative controls, the executed DSSE/TUF/OPA baselines, production GitHub Action workflows, benchmark scripts, and the paper figure generator. Furthermore, all datasets used to establish comparative baselines (e.g., the specific OPA Rego policies and JSON payloads used to evaluate the baseline) are included to guarantee full baseline reproducibility.

\subsection{Anonymous Review Artifact \& Verification}
To comply with USENIX Security '27 Open Science guidelines while preserving double-blind review, the complete artifact has been published to a sanitized, anonymous repository at:
\begin{center}
\url{https://anonymous.4open.science/r/eviassure-artifact-781D/}
\end{center}

The same content is also archived as \texttt{eviassure\_usenix27\_artifact.zip} (\artifactSizeKb{} KB), built by \nolinkurl{scripts/prepare_anonymous_artifact.py} with fixed member timestamps and sorted entries, so the archive is a deterministic function of the source rather than of the machine that packed it. Its SHA-256 is:
\begin{center}
\texttt{\artifactShaPartOne}\\
\texttt{\artifactShaPartTwo}
\end{center}
We state plainly what this digest does and does not give a reviewer. It fixes the archive we retain, and it lets a reader who obtains that archive confirm they hold the same bytes. It is \emph{not} independently checkable from the anonymous repository above: the archive is not served there, and the packager's forbidden-term list is deliberately withheld from the artifact (it is a list of author-identifying strings), so the build cannot be reproduced from the mirror alone. The reviewer-verifiable claims are the ones in the reproduction workflow below --- the test suite, and the \nolinkurl{results/} files every reported number is generated from.

\subsection{Reproduction Workflow}
To execute full reproduction of paper figures, benchmark results, and unit tests:
\begin{enumerate}
    \item \textbf{Environment Setup}: Install dependencies via \texttt{pip install -e ".[dev]"} (Python $\ge 3.10$); the \texttt{dev} extra supplies \texttt{pytest} and \texttt{hypothesis}, which the test suite imports.
    \item \textbf{Unit \& Integration Test Suite}: Run \texttt{pytest tests/ -v} (\testCount{} tests). The suite includes property-based crypto soundness tests, trusted-key-registry tests, the omission suite with its honest control, the credential-refusal and session-substitution tests, and an end-to-end test that drives \nolinkurl{scripts/verify_release_gate.py} against the shipped registry and policy; \texttt{test\_docs\_consistency.py} additionally binds the manuscript's stated test count to the collected count, so the two cannot drift.
    \item \textbf{Attestation Scaling \& Throughput Microbenchmarks}: Run \texttt{python3 scripts/run\_release\_benchmark.py --repeats 5} to generate \nolinkurl{results/benchmark_summary.json}.
    \item \textbf{Adversarial \& Baseline Evaluation}: Run \texttt{python3 scripts/run\_security\_eval.py {-}{-}require-executed} (with \texttt{python-tuf} installed and the \texttt{opa} binary on \texttt{PATH}) to execute the \scoredVectors{}-vector tamper suite, the clean negative controls, the wire-fuzzing campaign, the \omVectors{}-vector omission suite with its honest control, the executed DSSE/TUF/OPA baselines, the ablation matrix and the held-out inspection protocol into \nolinkurl{results/security_evaluation.json}, then \texttt{python3 scripts/write\_security\_macros.py}. These results are platform-independent. \texttt{{-}{-}require-executed} fails rather than silently substituting a modeled baseline, and the artifact packager refuses to ship a results file that records one.
    \item \textbf{Corpus Layered Evaluation}: Run \texttt{python3 scripts/generate\_trace\_corpus.py} then \texttt{python3 scripts/run\_corpus\_eval.py} to rebuild the \corpusProfiles{}-profile corpus and evaluate it through the integrity, policy and held-out inspection layers.
    \item \textbf{Figure Generation \& Manuscript PDF Rebuilding}: Run \texttt{python3 scripts/generate\_paper\_pdf.py} to regenerate paper plots and recompile \nolinkurl{docs/usenix_paper_manuscript.pdf}.
\end{enumerate}

\end{document}
